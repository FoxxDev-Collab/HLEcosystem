@model CreateLabelViewModel
@{
    ViewData["Title"] = "Create Label";
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Labels</a></li>
            <li class="breadcrumb-item active">Create</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Create Label</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        @Html.AntiForgeryToken()

                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Label Name</label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., Important, Warranty Active, Needs Upgrade" autofocus />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Color" class="form-label">Color (Optional)</label>
                            <div class="input-group">
                                <input asp-for="Color" class="form-control" type="color" style="max-width: 80px;" />
                                <input asp-for="Color" class="form-control" type="text" placeholder="#6c757d" maxlength="7" />
                            </div>
                            <span asp-validation-for="Color" class="text-danger"></span>
                            <small class="text-muted d-block mt-1">Hex color code for the label badge</small>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Create Label</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h6 class="mb-0">About Labels</h6>
                </div>
                <div class="card-body">
                    <p class="small mb-2">Labels are flexible tags you can apply to any asset regardless of category.</p>
                    <ul class="small mb-0">
                        <li>Cross-cutting organization</li>
                        <li>Filter assets by label</li>
                        <li>Apply multiple labels to one asset</li>
                        <li>Use colors for visual grouping</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const colorInputs = document.querySelectorAll('input[name="Color"]');
        if (colorInputs.length === 2) {
            colorInputs[0].addEventListener('input', function() {
                colorInputs[1].value = this.value;
            });
            colorInputs[1].addEventListener('input', function() {
                if (/^#[0-9A-Fa-f]{6}$/.test(this.value)) {
                    colorInputs[0].value = this.value;
                }
            });
        }
    </script>
}
