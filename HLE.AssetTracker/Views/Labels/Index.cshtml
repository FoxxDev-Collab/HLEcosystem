@model LabelIndexViewModel
@{
    ViewData["Title"] = "Labels";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Labels</h2>
        <a asp-action="Create" class="btn btn-primary">Add Label</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (Model.Labels.Any())
    {
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Label</th>
                                <th>Assets</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var label in Model.Labels)
                            {
                                <tr>
                                    <td>
                                        <span class="badge" style="background-color: @(label.Color ?? "#6c757d"); font-size: 0.9rem;">
                                            @label.Name
                                        </span>
                                    </td>
                                    <td>@label.AssetCount</td>
                                    <td class="text-end">
                                        <div class="btn-group">
                                            <a asp-action="Edit" asp-route-id="@label.Id" class="btn btn-sm btn-outline-primary">
                                                Edit
                                            </a>
                                            <form asp-action="Delete" asp-route-id="@label.Id" method="post" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-sm btn-outline-danger"
                                                        onclick="return confirm('@(label.AssetCount > 0 ? $"This label is used by {label.AssetCount} asset(s). Deleting it will remove the label from those assets. Continue?" : "Delete this label?")')">
                                                    Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body text-center py-5">
                <p class="text-muted mb-3">No labels created yet</p>
                <p class="small text-muted mb-3">Labels help you tag and organize assets across categories</p>
                <a asp-action="Create" class="btn btn-primary">Create First Label</a>
            </div>
        </div>
    }
</div>
