@using HLE.AssetTracker.Services.Interfaces
@using HLE.AssetTracker.Models.Entities
@{
    ViewData["Title"] = "Dashboard";
    var stats = ViewData["Stats"] as DashboardStatsDto;
    var recentAssets = ViewData["RecentAssets"] as List<AssetListItemDto> ?? [];
    var expiringWarranties = ViewData["ExpiringWarranties"] as List<Asset> ?? [];
    var householdName = ViewData["HouseholdName"] as string ?? "My Home";
    var userName = ViewData["UserName"] as string ?? "User";
}

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col">
            <h2 class="mb-1">Welcome back, @userName!</h2>
            <p class="text-muted">@householdName</p>
        </div>
        <div class="col-auto">
            <a asp-controller="Assets" asp-action="Create" class="btn btn-primary">
                + Add Asset
            </a>
        </div>
    </div>

    <!-- Stats Cards -->
    <div class="row g-3 mb-4">
        <div class="col-md-6 col-lg-3">
            <div class="card bg-primary">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-subtitle mb-1 opacity-50">Total Assets</h6>
                            <h2 class="card-title mb-0">@(stats?.TotalAssets ?? 0)</h2>
                        </div>
                        <div class="fs-1 opacity-50">
                            <i data-lucide="package" style="width: 2rem; height: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-lg-3">
            <div class="card bg-success">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-subtitle mb-1 opacity-50">Total Value</h6>
                            <h2 class="card-title mb-0">$@(stats?.TotalValue.ToString("N0") ?? "0")</h2>
                        </div>
                        <div class="fs-1 opacity-50">
                            <i data-lucide="dollar-sign" style="width: 2rem; height: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-lg-3">
            <div class="card @(stats?.ExpiringWarrantiesCount > 0 ? "bg-warning" : "bg-secondary")">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-subtitle mb-1 opacity-50">Expiring Warranties</h6>
                            <h2 class="card-title mb-0">@(stats?.ExpiringWarrantiesCount ?? 0)</h2>
                        </div>
                        <div class="fs-1 opacity-50">
                            <i data-lucide="shield-check" style="width: 2rem; height: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-lg-3">
            <div class="card @(stats?.UpcomingMaintenanceCount > 0 ? "bg-info" : "bg-secondary")">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-subtitle mb-1 opacity-50">Upcoming Maintenance</h6>
                            <h2 class="card-title mb-0">@(stats?.UpcomingMaintenanceCount ?? 0)</h2>
                        </div>
                        <div class="fs-1 opacity-50">
                            <i data-lucide="wrench" style="width: 2rem; height: 2rem;"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <!-- Recent Assets -->
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Recent Assets</h5>
                    <a asp-controller="Assets" asp-action="Index" class="btn btn-sm btn-outline-primary">View All</a>
                </div>
                <div class="card-body p-0">
                    @if (recentAssets.Count == 0)
                    {
                        <div class="p-4 text-center text-muted">
                            <p class="mb-2">No assets yet.</p>
                            <a asp-controller="Assets" asp-action="Create" class="btn btn-primary btn-sm">Add your first asset</a>
                        </div>
                    }
                    else
                    {
                        <div class="table-responsive">
                            <table class="table table-hover mb-0">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Category</th>
                                        <th>Location</th>
                                        <th class="text-end">Value</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var asset in recentAssets)
                                    {
                                        <tr>
                                            <td>
                                                <a asp-controller="Assets" asp-action="Details" asp-route-id="@asset.Id" class="text-decoration-none">
                                                    @asset.Name
                                                </a>
                                            </td>
                                            <td class="text-muted">@(asset.CategoryName ?? "-")</td>
                                            <td class="text-muted">@(asset.LocationName ?? "-")</td>
                                            <td class="text-end">
                                                @if (asset.PurchasePrice.HasValue)
                                                {
                                                    @:$@asset.PurchasePrice.Value.ToString("N2")
                                                }
                                                else
                                                {
                                                    <span class="text-muted">-</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- Expiring Warranties -->
        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Expiring Warranties</h5>
                    <small class="text-muted">Next 30 days</small>
                </div>
                <div class="card-body p-0">
                    @if (expiringWarranties.Count == 0)
                    {
                        <div class="p-4 text-center text-muted">
                            <p class="mb-0">No warranties expiring soon.</p>
                        </div>
                    }
                    else
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var asset in expiringWarranties.Take(5))
                            {
                                var daysUntilExpiry = (asset.WarrantyExpiration!.Value.ToDateTime(TimeOnly.MinValue) - DateTime.UtcNow).Days;
                                var badgeClass = daysUntilExpiry <= 7 ? "bg-danger" : daysUntilExpiry <= 14 ? "bg-warning" : "bg-secondary";
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    <div>
                                        <a asp-controller="Assets" asp-action="Details" asp-route-id="@asset.Id" class="text-decoration-none">
                                            @asset.Name
                                        </a>
                                        <br />
                                        <small class="text-muted">Expires @asset.WarrantyExpiration.Value.ToString("MMM d, yyyy")</small>
                                    </div>
                                    <span class="badge @badgeClass">@daysUntilExpiry days</span>
                                </li>
                            }
                        </ul>
                    }
                </div>
            </div>

            <!-- Quick Stats -->
            <div class="card mt-4">
                <div class="card-header">
                    <h5 class="mb-0">Organization</h5>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        Locations
                        <span class="badge bg-primary rounded-pill">@(stats?.LocationsCount ?? 0)</span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        Categories
                        <span class="badge bg-primary rounded-pill">@(stats?.CategoriesCount ?? 0)</span>
                    </li>
                </ul>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-controller="Locations" asp-action="Index" class="btn btn-outline-secondary btn-sm">Manage Locations</a>
                        <a asp-controller="Categories" asp-action="Index" class="btn btn-outline-secondary btn-sm">Manage Categories</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
