@model IEnumerable<HLE.AssetTracker.Models.Entities.Vehicle>
@{
    ViewData["Title"] = "Vehicles";
}

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="mb-0">My Vehicles</h1>
                <a asp-action="Create" class="btn btn-primary">
                    <i data-lucide="plus" style="width: 16px; height: 16px;"></i>
                    Add Vehicle
                </a>
            </div>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (Model != null && Model.Any())
    {
        <div class="row">
            @foreach (var vehicle in Model)
            {
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-start mb-3">
                                <i data-lucide="car" style="width: 32px; height: 32px;" class="text-primary me-3"></i>
                                <div class="flex-grow-1">
                                    <h5 class="card-title mb-1">@vehicle.Year @vehicle.Make @vehicle.Model</h5>
                                    @if (!string.IsNullOrEmpty(vehicle.VehicleType))
                                    {
                                        <span class="badge bg-secondary">@vehicle.VehicleType</span>
                                    }
                                </div>
                            </div>

                            <div class="mb-3">
                                @if (!string.IsNullOrEmpty(vehicle.VIN))
                                {
                                    <p class="mb-1 text-muted small">
                                        <strong>VIN:</strong> @vehicle.VIN
                                    </p>
                                }
                                @if (!string.IsNullOrEmpty(vehicle.LicensePlate))
                                {
                                    <p class="mb-1 text-muted small">
                                        <strong>License:</strong> @vehicle.LicensePlate
                                    </p>
                                }
                                @if (!string.IsNullOrEmpty(vehicle.Color))
                                {
                                    <p class="mb-1 text-muted small">
                                        <strong>Color:</strong> @vehicle.Color
                                    </p>
                                }
                                <p class="mb-1">
                                    <strong>Odometer:</strong> @vehicle.CurrentOdometer.ToString("N0") @vehicle.OdometerUnit
                                </p>
                            </div>

                            <div class="d-flex gap-2">
                                <a asp-action="Details" asp-route-id="@vehicle.Id" class="btn btn-sm btn-primary flex-grow-1">
                                    <i data-lucide="eye" style="width: 14px; height: 14px;"></i>
                                    View Details
                                </a>
                                <a asp-action="Edit" asp-route-id="@vehicle.Id" class="btn btn-sm btn-outline-secondary">
                                    <i data-lucide="edit" style="width: 14px; height: 14px;"></i>
                                </a>
                                @if (vehicle.IsArchived)
                                {
                                    <form asp-action="Restore" asp-route-id="@vehicle.Id" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-outline-success" title="Restore">
                                            <i data-lucide="archive-restore" style="width: 14px; height: 14px;"></i>
                                        </button>
                                    </form>
                                }
                                else
                                {
                                    <form asp-action="Archive" asp-route-id="@vehicle.Id" method="post" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-outline-warning" title="Archive" onclick="return confirm('Archive this vehicle?')">
                                            <i data-lucide="archive" style="width: 14px; height: 14px;"></i>
                                        </button>
                                    </form>
                                }
                                <form asp-action="Delete" asp-route-id="@vehicle.Id" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete Permanently" onclick="return confirm('⚠️ PERMANENTLY DELETE this vehicle? This cannot be undone!')">
                                        <i data-lucide="trash-2" style="width: 14px; height: 14px;"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body text-center py-5">
                        <i data-lucide="car" style="width: 64px; height: 64px;" class="text-muted mb-3"></i>
                        <h4>No Vehicles Yet</h4>
                        <p class="text-muted mb-4">Start tracking your vehicles by adding one now!</p>
                        <a asp-action="Create" class="btn btn-primary">
                            <i data-lucide="plus" style="width: 16px; height: 16px;"></i>
                            Add Your First Vehicle
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            lucide.createIcons();
        });
    </script>
}
