@model MaintenanceIndexViewModel
@{
    ViewData["Title"] = "Maintenance Dashboard";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Maintenance Dashboard</h2>
        <a asp-controller="Assets" asp-action="Index" class="btn btn-outline-secondary">View Assets</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Stats Overview -->
    <div class="row g-3 mb-4">
        <div class="col-md-3">
            <div class="card border-danger">
                <div class="card-body text-center">
                    <h3 class="text-danger mb-0">@Model.Dashboard.OverdueCount</h3>
                    <small class="text-muted">Overdue</small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-warning">
                <div class="card-body text-center">
                    <h3 class="text-warning mb-0">@Model.Dashboard.DueTodayCount</h3>
                    <small class="text-muted">Due Today</small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-info">
                <div class="card-body text-center">
                    <h3 class="text-info mb-0">@Model.Dashboard.DueThisWeekCount</h3>
                    <small class="text-muted">This Week</small>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-secondary">
                <div class="card-body text-center">
                    <h3 class="text-secondary mb-0">@Model.Dashboard.DueThisMonthCount</h3>
                    <small class="text-muted">This Month</small>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-6 mb-4">
            <!-- Overdue Tasks -->
            @if (Model.Dashboard.OverdueTasks.Any())
            {
                <div class="card border-danger mb-4">
                    <div class="card-header bg-danger text-white">
                        <h5 class="mb-0">Overdue Maintenance</h5>
                    </div>
                    <div class="list-group list-group-flush">
                        @foreach (var task in Model.Dashboard.OverdueTasks)
                        {
                            <div class="list-group-item">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1">@task.Name</h6>
                                        <small class="text-muted">@task.AssetName</small>
                                        <div class="mt-1">
                                            <span class="badge bg-danger">@Math.Abs(task.DaysUntilDue) days overdue</span>
                                        </div>
                                    </div>
                                    <div class="btn-group">
                                        <form asp-action="CompleteSchedule" asp-route-id="@task.Id" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-success" title="Mark Complete">
                                                ✓
                                            </button>
                                        </form>
                                        <a asp-action="LogMaintenance" asp-route-assetId="@task.AssetId" asp-route-scheduleId="@task.Id"
                                           class="btn btn-sm btn-outline-primary" title="Log Details">
                                            Log
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }

            <!-- Due Today -->
            @if (Model.Dashboard.DueTodayTasks.Any())
            {
                <div class="card border-warning mb-4">
                    <div class="card-header bg-warning">
                        <h5 class="mb-0">Due Today</h5>
                    </div>
                    <div class="list-group list-group-flush">
                        @foreach (var task in Model.Dashboard.DueTodayTasks)
                        {
                            <div class="list-group-item">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1">@task.Name</h6>
                                        <small class="text-muted">@task.AssetName</small>
                                    </div>
                                    <div class="btn-group">
                                        <form asp-action="CompleteSchedule" asp-route-id="@task.Id" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-success" title="Mark Complete">
                                                ✓
                                            </button>
                                        </form>
                                        <a asp-action="LogMaintenance" asp-route-assetId="@task.AssetId" asp-route-scheduleId="@task.Id"
                                           class="btn btn-sm btn-outline-primary" title="Log Details">
                                            Log
                                        </a>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }

            <!-- Upcoming -->
            @if (Model.Dashboard.UpcomingTasks.Any())
            {
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Upcoming (30 Days)</h5>
                    </div>
                    <div class="list-group list-group-flush">
                        @foreach (var task in Model.Dashboard.UpcomingTasks)
                        {
                            <div class="list-group-item">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1">@task.Name</h6>
                                        <small class="text-muted">@task.AssetName</small>
                                        <div class="mt-1">
                                            <small class="text-muted">Due @task.NextDueDate.ToString("MMM d") (@task.DaysUntilDue days)</small>
                                        </div>
                                    </div>
                                    <a asp-action="ByAsset" asp-route-id="@task.AssetId" class="btn btn-sm btn-outline-secondary">
                                        View
                                    </a>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            }

            @if (!Model.Dashboard.OverdueTasks.Any() && !Model.Dashboard.DueTodayTasks.Any() && !Model.Dashboard.UpcomingTasks.Any())
            {
                <div class="card">
                    <div class="card-body text-center py-5">
                        <p class="text-muted mb-3">No scheduled maintenance tasks</p>
                        <a asp-controller="Assets" asp-action="Index" class="btn btn-primary">
                            Add Maintenance to Assets
                        </a>
                    </div>
                </div>
            }
        </div>

        <div class="col-lg-6">
            <!-- Recent Activity -->
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Recent Activity</h5>
                    <a asp-action="History" class="btn btn-sm btn-outline-secondary">View All</a>
                </div>
                @if (Model.Dashboard.RecentLogs.Any())
                {
                    <div class="list-group list-group-flush">
                        @foreach (var log in Model.Dashboard.RecentLogs)
                        {
                            <div class="list-group-item">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <h6 class="mb-1">@log.Description</h6>
                                        <small class="text-muted">@log.AssetName</small>
                                        <div class="mt-1">
                                            <small class="text-muted">@log.PerformedAt.ToString("MMM d, yyyy")</small>
                                            @if (log.Cost.HasValue)
                                            {
                                                <span class="ms-2 badge bg-secondary">$@log.Cost.Value.ToString("N2")</span>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <div class="card-body text-center py-5">
                        <p class="text-muted mb-0">No maintenance history yet</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
