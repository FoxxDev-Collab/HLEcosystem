@model LogMaintenanceViewModel
@{
    ViewData["Title"] = "Log Maintenance";
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Assets" asp-action="Index">Assets</a></li>
            <li class="breadcrumb-item"><a asp-controller="Assets" asp-action="Details" asp-route-id="@Model.AssetId">@Model.AssetName</a></li>
            <li class="breadcrumb-item"><a asp-action="ByAsset" asp-route-id="@Model.AssetId">Maintenance</a></li>
            <li class="breadcrumb-item active">Log Maintenance</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Log Maintenance</h4>
                    <small class="text-muted">Asset: @Model.AssetName</small>
                    @if (!string.IsNullOrWhiteSpace(Model.ScheduleName))
                    {
                        <small class="text-muted d-block">Schedule: @Model.ScheduleName</small>
                    }
                </div>
                <div class="card-body">
                    <form asp-action="LogMaintenance" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="AssetId" />
                        <input type="hidden" asp-for="ScheduleId" />

                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="PerformedAt" class="form-label">Date Performed</label>
                            <input asp-for="PerformedAt" class="form-control" type="date" />
                            <span asp-validation-for="PerformedAt" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Description</label>
                            <input asp-for="Description" class="form-control" placeholder="What maintenance was performed?" />
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Cost" class="form-label">Cost (Optional)</label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input asp-for="Cost" class="form-control" type="number" step="0.01" min="0" placeholder="0.00" />
                            </div>
                            <span asp-validation-for="Cost" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Notes" class="form-label">Notes (Optional)</label>
                            <textarea asp-for="Notes" class="form-control" rows="4" placeholder="Additional details, parts used, observations, etc."></textarea>
                            <span asp-validation-for="Notes" class="text-danger"></span>
                        </div>

                        @if (Model.ScheduleId.HasValue)
                        {
                            <div class="alert alert-info">
                                <strong>Note:</strong> Logging this maintenance will mark the schedule as complete and advance the next due date.
                            </div>
                        }

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Log Maintenance</button>
                            <a asp-action="ByAsset" asp-route-id="@Model.AssetId" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h6 class="mb-0">Tips</h6>
                </div>
                <div class="card-body">
                    <ul class="small mb-0">
                        <li>Record the actual date the work was completed</li>
                        <li>Include costs to track total maintenance expenses</li>
                        <li>Add detailed notes for future reference</li>
                        <li>Document parts used or observations</li>
                        @if (Model.ScheduleId.HasValue)
                        {
                            <li>The schedule's next due date will automatically advance</li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
