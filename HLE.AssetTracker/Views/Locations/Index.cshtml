@model LocationIndexViewModel
@{
    ViewData["Title"] = "Locations";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Locations", null, true)
    };
}
<partial name="_Breadcrumb" />

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Locations</h2>
        <a asp-action="Create" class="btn btn-primary">Add Location</a>
    </div>

    @if (TempData["Success"] != null)
    {
        ViewData["Message"] = TempData["Success"];
        ViewData["Variant"] = "success";
        ViewData["Dismissible"] = "True";
        <partial name="_Alert" />
    }
    @if (TempData["Error"] != null)
    {
        ViewData["Message"] = TempData["Error"];
        ViewData["Variant"] = "danger";
        ViewData["Dismissible"] = "True";
        <partial name="_Alert" />
    }

    @if (Model.Locations.Any())
    {
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">Location Hierarchy</h5>
            </div>
            <div class="card-body">
                <div class="location-tree">
                    @foreach (var location in Model.Locations)
                    {
                        @await Html.PartialAsync("_LocationTree", location)
                    }
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body text-center py-5">
                <p class="text-muted mb-3">No locations created yet</p>
                <a asp-action="Create" class="btn btn-primary">Create First Location</a>
            </div>
        </div>
    }
</div>

<style>
    .location-tree {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    }

    .location-item {
        margin-bottom: 0.5rem;
    }

    .location-header {
        display: flex;
        align-items: center;
        padding: 0.75rem;
        border: 1px solid #dee2e6;
        border-radius: 0.375rem;
        background-color: #f8f9fa;
        transition: background-color 0.2s;
    }

    .location-header:hover {
        background-color: #e9ecef;
    }

    .location-children {
        margin-left: 2rem;
        margin-top: 0.5rem;
        border-left: 2px solid #dee2e6;
        padding-left: 1rem;
    }

    .location-name {
        flex-grow: 1;
        font-weight: 500;
    }

    .location-description {
        font-size: 0.875rem;
        color: #6c757d;
        margin-left: 1rem;
    }

    .location-actions {
        display: flex;
        gap: 0.5rem;
    }
</style>
