@model HLE.AssetTracker.Models.ViewModels.AssetCreateViewModel
@{
    ViewData["Title"] = "Add Asset";
    var categories = ViewData["Categories"] as SelectList;
    var locations = ViewData["Locations"] as SelectList;
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Add Asset</h2>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>

            <form id="assetCreateForm" asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                @* Validation Summary - Standard ASP.NET Core approach *@
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                @* Debug: Show detailed ModelState errors if present *@
                @if (ViewData.ModelState.ErrorCount > 0)
                {
                    <div class="alert alert-warning">
                        <strong>Debug - Validation Errors (@ViewData.ModelState.ErrorCount):</strong>
                        <ul class="mb-0">
                            @foreach (var key in ViewData.ModelState.Keys)
                            {
                                var state = ViewData.ModelState[key];
                                if (state?.Errors.Count > 0)
                                {
                                    <li><strong>@key:</strong>
                                        @foreach (var error in state.Errors)
                                        {
                                            <span>@error.ErrorMessage; </span>
                                        }
                                    </li>
                                }
                            }
                        </ul>
                    </div>
                }

                <!-- Basic Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Basic Information</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Name <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., Samsung TV, Kitchen Table" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="3" placeholder="Additional details about this asset..."></textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="CategoryId" class="form-label">Category</label>
                                <select asp-for="CategoryId" class="form-select" asp-items="@categories">
                                    <option value="">Select category...</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="LocationId" class="form-label">Location</label>
                                <select asp-for="LocationId" class="form-select" asp-items="@locations">
                                    <option value="">Select location...</option>
                                </select>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Quantity" class="form-label">Quantity</label>
                            <input asp-for="Quantity" type="number" class="form-control" min="1" style="max-width: 120px;" />
                        </div>
                    </div>
                </div>

                <!-- Product Details -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Product Details</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Manufacturer" class="form-label">Manufacturer / Brand</label>
                                <input asp-for="Manufacturer" class="form-control" placeholder="e.g., Samsung, IKEA" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="Model" class="form-label">Model</label>
                                <input asp-for="Model" class="form-control" placeholder="e.g., UN55TU8000" />
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="SerialNumber" class="form-label">Serial Number</label>
                            <input asp-for="SerialNumber" class="form-control" placeholder="Serial number or product ID" />
                        </div>
                    </div>
                </div>

                <!-- Purchase Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Purchase Information</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="PurchaseDate" class="form-label">Purchase Date</label>
                                <input asp-for="PurchaseDate" type="date" class="form-control" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="PurchasePrice" class="form-label">Purchase Price</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="PurchasePrice" type="number" step="0.01" class="form-control" placeholder="0.00" />
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="PurchaseLocation" class="form-label">Purchase Location</label>
                            <input asp-for="PurchaseLocation" class="form-control" placeholder="e.g., Amazon, Best Buy, Local Store" />
                        </div>
                    </div>
                </div>

                <!-- Warranty Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Warranty</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="WarrantyExpiration" class="form-label">Warranty Expiration</label>
                            <input asp-for="WarrantyExpiration" type="date" class="form-control" style="max-width: 200px;" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="WarrantyNotes" class="form-label">Warranty Notes</label>
                            <textarea asp-for="WarrantyNotes" class="form-control" rows="2" placeholder="Coverage details, claim info, etc."></textarea>
                        </div>
                    </div>
                </div>

                <!-- Notes -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Notes</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-0">
                            <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Any additional notes..."></textarea>
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Create Asset</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function() {
            console.log('=== Form Validation Debug ===');
            console.log('jQuery validation loaded:', typeof $.validator !== 'undefined');
            console.log('Unobtrusive validation loaded:', typeof $.validator.unobtrusive !== 'undefined');

            var $form = $('#assetCreateForm');
            console.log('Asset form found:', $form.length);
            console.log('Total forms on page:', $('form').length);

            // Log form validation on submit
            $form.on('submit', function(e) {
                console.log('Asset form submit event triggered');
                var isValid = $(this).valid();
                console.log('Asset form valid:', isValid);

                if (!isValid) {
                    console.log('Client-side validation failed - preventing submit');
                    var errors = $(this).validate().errorList;
                    console.log('Validation errors:', errors);
                }
            });

            // Log Name field value on blur
            $('#Name').on('blur', function() {
                console.log('Name field value:', $(this).val());
                console.log('Name field length:', $(this).val().length);
            });
        });
    </script>
}
