@model HLE.AssetTracker.Models.ViewModels.AssetEditViewModel
@{
    ViewData["Title"] = "Edit Asset";
    var categories = ViewData["Categories"] as SelectList;
    var locations = ViewData["Locations"] as SelectList;
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2>Edit Asset</h2>
                <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">Cancel</a>
            </div>

            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger">
                        <ul class="mb-0">
                            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <li>@error.ErrorMessage</li>
                            }
                        </ul>
                    </div>
                }

                <!-- Basic Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Basic Information</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Name <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="CategoryId" class="form-label">Category</label>
                                <select asp-for="CategoryId" class="form-select" asp-items="@categories">
                                    <option value="">Select category...</option>
                                </select>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="LocationId" class="form-label">Location</label>
                                <select asp-for="LocationId" class="form-select" asp-items="@locations">
                                    <option value="">Select location...</option>
                                </select>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Quantity" class="form-label">Quantity</label>
                            <input asp-for="Quantity" type="number" class="form-control" min="1" style="max-width: 120px;" />
                        </div>
                    </div>
                </div>

                <!-- Product Details -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Product Details</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Manufacturer" class="form-label">Manufacturer / Brand</label>
                                <input asp-for="Manufacturer" class="form-control" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="Model" class="form-label">Model</label>
                                <input asp-for="Model" class="form-control" />
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="SerialNumber" class="form-label">Serial Number</label>
                            <input asp-for="SerialNumber" class="form-control" />
                        </div>
                    </div>
                </div>

                <!-- Purchase Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Purchase Information</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="PurchaseDate" class="form-label">Purchase Date</label>
                                <input asp-for="PurchaseDate" type="date" class="form-control" />
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="PurchasePrice" class="form-label">Purchase Price</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="PurchasePrice" type="number" step="0.01" class="form-control" />
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label asp-for="PurchaseLocation" class="form-label">Purchase Location</label>
                            <input asp-for="PurchaseLocation" class="form-control" />
                        </div>
                    </div>
                </div>

                <!-- Warranty Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Warranty</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <label asp-for="WarrantyExpiration" class="form-label">Warranty Expiration</label>
                            <input asp-for="WarrantyExpiration" type="date" class="form-control" style="max-width: 200px;" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="WarrantyNotes" class="form-label">Warranty Notes</label>
                            <textarea asp-for="WarrantyNotes" class="form-control" rows="2"></textarea>
                        </div>
                    </div>
                </div>

                <!-- Notes -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Notes</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-0">
                            <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                        </div>
                    </div>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
