@model CategoryIndexViewModel
@{
    ViewData["Title"] = "Categories";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Categories", null, true)
    };
}
<partial name="_Breadcrumb" />

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Categories</h2>
        <a asp-action="Create" class="btn btn-primary">Add Category</a>
    </div>

    @if (TempData["Success"] != null)
    {
        ViewData["Message"] = TempData["Success"];
        ViewData["Variant"] = "success";
        ViewData["Dismissible"] = "True";
        <partial name="_Alert" />
    }
    @if (TempData["Error"] != null)
    {
        ViewData["Message"] = TempData["Error"];
        ViewData["Variant"] = "danger";
        ViewData["Dismissible"] = "True";
        <partial name="_Alert" />
    }

    @if (Model.Categories.Any())
    {
        <div class="row g-3">
            @foreach (var category in Model.Categories)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <div class="d-flex align-items-start mb-3">
                                @if (!string.IsNullOrWhiteSpace(category.Icon))
                                {
                                    <span class="fs-3 me-2" style="@(!string.IsNullOrWhiteSpace(category.Color) ? $"color: {category.Color};" : "")">
                                        @category.Icon
                                    </span>
                                }
                                <div class="flex-grow-1">
                                    <h5 class="card-title mb-1">@category.Name</h5>
                                    <div class="d-flex gap-2 flex-wrap">
                                        <span class="badge bg-secondary">@category.AssetCount asset@(category.AssetCount == 1 ? "" : "s")</span>
                                        @if (category.CustomFieldCount > 0)
                                        {
                                            <span class="badge bg-info">@category.CustomFieldCount custom field@(category.CustomFieldCount == 1 ? "" : "s")</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-transparent">
                            <div class="d-flex gap-2">
                                <a asp-action="Edit" asp-route-id="@category.Id" class="btn btn-sm btn-outline-primary flex-grow-1">
                                    Edit & Custom Fields
                                </a>
                                <form asp-action="Delete" asp-route-id="@category.Id" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                            onclick="return confirm('@(category.AssetCount > 0 ? $"This category has {category.AssetCount} asset(s). Deleting it will remove the category from those assets. Continue?" : "Delete this category?")')">
                                        Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body text-center py-5">
                <p class="text-muted mb-3">No categories created yet</p>
                <a asp-action="Create" class="btn btn-primary">Create First Category</a>
            </div>
        </div>
    }
</div>
