@model CreateCategoryViewModel
@{
    ViewData["Title"] = "Create Category";
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Categories</a></li>
            <li class="breadcrumb-item active">Create</li>
        </ol>
    </nav>

    <div class="row">
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Create Category</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        @Html.AntiForgeryToken()

                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Category Name</label>
                            <input asp-for="Name" class="form-control" placeholder="e.g., Electronics, Furniture, Appliances" autofocus />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Icon" class="form-label">Icon (Optional)</label>
                            <input asp-for="Icon" class="form-control" placeholder="e.g., 📱, 🪑, 🔧" maxlength="10" />
                            <span asp-validation-for="Icon" class="text-danger"></span>
                            <small class="text-muted d-block mt-1">Use an emoji or short icon symbol</small>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Color" class="form-label">Color (Optional)</label>
                            <div class="input-group">
                                <input asp-for="Color" class="form-control" type="color" style="max-width: 80px;" />
                                <input asp-for="Color" class="form-control" type="text" placeholder="#000000" maxlength="7" />
                            </div>
                            <span asp-validation-for="Color" class="text-danger"></span>
                            <small class="text-muted d-block mt-1">Hex color code for visual identification</small>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Create Category</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h6 class="mb-0">About Categories</h6>
                </div>
                <div class="card-body">
                    <p class="small mb-2">Categories help you organize your assets by type.</p>
                    <ul class="small mb-0">
                        <li>Group similar assets together</li>
                        <li>Add custom fields specific to each category</li>
                        <li>Use icons and colors for quick visual identification</li>
                        <li>Edit categories later to add custom fields</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Sync color picker with text input
        const colorInput = document.querySelectorAll('input[name="Color"]');
        if (colorInput.length === 2) {
            colorInput[0].addEventListener('input', function() {
                colorInput[1].value = this.value;
            });
            colorInput[1].addEventListener('input', function() {
                if (/^#[0-9A-Fa-f]{6}$/.test(this.value)) {
                    colorInput[0].value = this.value;
                }
            });
        }
    </script>
}
