@model HLE.FamilyHealth.Models.ViewModels.ProviderViewModel
@{
    ViewData["Title"] = "Edit Healthcare Provider";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="d-flex align-items-center mb-4">
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <div>
                    <h2 class="mb-1">Edit Healthcare Provider</h2>
                    <p class="text-muted mb-0">Update provider information for @Model.Name</p>
                </div>
            </div>

            @if (TempData["ErrorMessage"] != null)
            {
                ViewData["AlertType"] = "danger";
                ViewData["AlertMessage"] = TempData["ErrorMessage"]?.ToString();
                ViewData["AlertDismissible"] = true;
                <partial name="_Alert" />
            }

            <div class="card">
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="row g-3">
                            <div class="col-12">
                                <h5>Basic Information</h5>
                            </div>

                            <div class="col-md-8">
                                <label asp-for="Name" class="form-label">Provider Name</label>
                                <input asp-for="Name" class="form-control" placeholder="e.g., Dr. John Smith or Main Street Hospital" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="Type" class="form-label">Provider Type</label>
                                <select asp-for="Type" class="form-select">
                                    <option value="Doctor">Doctor</option>
                                    <option value="Dentist">Dentist</option>
                                    <option value="Specialist">Specialist</option>
                                    <option value="Hospital">Hospital</option>
                                    <option value="Urgent Care">Urgent Care</option>
                                    <option value="Lab">Lab</option>
                                    <option value="Pharmacy">Pharmacy</option>
                                    <option value="Mental Health">Mental Health</option>
                                    <option value="Physical Therapy">Physical Therapy</option>
                                    <option value="Other">Other</option>
                                </select>
                                <span asp-validation-for="Type" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <label asp-for="Specialty" class="form-label">Specialty</label>
                                <input asp-for="Specialty" class="form-control" placeholder="e.g., Cardiology, Family Medicine, Orthopedics" />
                                <span asp-validation-for="Specialty" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <label asp-for="Address" class="form-label">Address</label>
                                <textarea asp-for="Address" class="form-control" rows="3" placeholder="Full address including street, city, state, zip"></textarea>
                                <span asp-validation-for="Address" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                                <h5>Contact Information</h5>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="PhoneNumber" class="form-label">Phone Number</label>
                                <input asp-for="PhoneNumber" type="tel" class="form-control" placeholder="(555) 123-4567" />
                                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="FaxNumber" class="form-label">Fax Number</label>
                                <input asp-for="FaxNumber" type="tel" class="form-control" placeholder="(555) 987-6543" />
                                <span asp-validation-for="FaxNumber" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="Email" class="form-label">Email Address</label>
                                <input asp-for="Email" type="email" class="form-control" placeholder="contact@provider.com" />
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="PreferredContactMethod" class="form-label">Preferred Contact Method</label>
                                <select asp-for="PreferredContactMethod" class="form-select">
                                    <option value="">Select method</option>
                                    <option value="Phone">Phone</option>
                                    <option value="Email">Email</option>
                                    <option value="Fax">Fax</option>
                                    <option value="Portal">Patient Portal</option>
                                </select>
                                <span asp-validation-for="PreferredContactMethod" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                                <h5>Online Access</h5>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="Website" class="form-label">Website</label>
                                <input asp-for="Website" type="url" class="form-control" placeholder="https://www.provider.com" />
                                <span asp-validation-for="Website" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="PortalUrl" class="form-label">Patient Portal URL</label>
                                <input asp-for="PortalUrl" type="url" class="form-control" placeholder="https://portal.provider.com" />
                                <span asp-validation-for="PortalUrl" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                            </div>

                            <div class="col-12">
                                <label asp-for="Notes" class="form-label">Notes</label>
                                <textarea asp-for="Notes" class="form-control" rows="4" placeholder="Additional notes about this provider (office hours, parking info, etc.)"></textarea>
                                <span asp-validation-for="Notes" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <div class="form-check form-switch">
                                    <input asp-for="IsActive" class="form-check-input" type="checkbox" />
                                    <label asp-for="IsActive" class="form-check-label">Active Provider</label>
                                </div>
                                <span asp-validation-for="IsActive" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mt-4 d-flex gap-2">
                            @{
                                ViewData["Text"] = "Save Changes";
                                ViewData["Variant"] = "primary";
                                ViewData["Type"] = "submit";
                                ViewData["Icon"] = "bi bi-check-lg";
                            }
                            <partial name="_Button" />

                            @{
                                ViewData["Text"] = "Cancel";
                                ViewData["Variant"] = "outline-secondary";
                                ViewData["Href"] = Url.Action("Index");
                            }
                            <partial name="_Button" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
