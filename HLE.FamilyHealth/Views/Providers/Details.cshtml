@model HLE.FamilyHealth.Models.Entities.Provider
@{
    ViewData["Title"] = "Provider Details";
}

<div class="container-fluid py-4">
    <div class="d-flex align-items-center mb-4">
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
            <i class="bi bi-arrow-left"></i>
        </a>
        <div class="flex-grow-1">
            <div class="d-flex align-items-center justify-content-between">
                <div>
                    <h2 class="mb-1">@Model.Name</h2>
                    <p class="text-muted mb-0">
                        @Model.Type
                        @if (!string.IsNullOrEmpty(Model.Specialty))
                        {
                            <span> - @Model.Specialty</span>
                        }
                        @if (Model.IsActive)
                        {
                            ViewData["BadgeText"] = "Active";
                            ViewData["BadgeVariant"] = "success";
                            ViewData["BadgePill"] = "True";
                            <partial name="_Badge" />
                        }
                        else
                        {
                            ViewData["BadgeText"] = "Inactive";
                            ViewData["BadgeVariant"] = "secondary";
                            ViewData["BadgePill"] = "True";
                            <partial name="_Badge" />
                        }
                    </p>
                </div>
                <div>
                    @{
                        ViewData["Text"] = "Edit";
                        ViewData["Variant"] = "primary";
                        ViewData["Href"] = Url.Action("Edit", new { id = Model.Id });
                        ViewData["Icon"] = "bi bi-pencil";
                    }
                    <partial name="_Button" />
                </div>
            </div>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    <div class="row g-4">
        <div class="col-lg-6">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="mb-0">Provider Information</h5>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">Name:</dt>
                        <dd class="col-sm-7">@Model.Name</dd>

                        <dt class="col-sm-5">Type:</dt>
                        <dd class="col-sm-7">
                            @{
                                var typeVariant = Model.Type switch
                                {
                                    "Doctor" => "primary",
                                    "Dentist" => "info",
                                    "Hospital" => "danger",
                                    "Pharmacy" => "success",
                                    _ => "secondary"
                                };
                                ViewData["BadgeText"] = Model.Type;
                                ViewData["BadgeVariant"] = typeVariant;
                            }
                            <partial name="_Badge" />
                        </dd>

                        <dt class="col-sm-5">Specialty:</dt>
                        <dd class="col-sm-7">@(Model.Specialty ?? "-")</dd>

                        <dt class="col-sm-5">Status:</dt>
                        <dd class="col-sm-7">
                            @if (Model.IsActive)
                            {
                                ViewData["BadgeText"] = "Active";
                                ViewData["BadgeVariant"] = "success";
                                <partial name="_Badge" />
                            }
                            else
                            {
                                ViewData["BadgeText"] = "Inactive";
                                ViewData["BadgeVariant"] = "secondary";
                                <partial name="_Badge" />
                            }
                        </dd>

                        @if (!string.IsNullOrEmpty(Model.PreferredContactMethod))
                        {
                            <dt class="col-sm-5">Preferred Contact:</dt>
                            <dd class="col-sm-7">
                                @{
                                    ViewData["BadgeText"] = Model.PreferredContactMethod;
                                    ViewData["BadgeVariant"] = "info";
                                    ViewData["BadgePill"] = "True";
                                }
                                <partial name="_Badge" />
                            </dd>
                        }
                    </dl>
                </div>
            </div>
        </div>

        <div class="col-lg-6">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="mb-0">Contact Information</h5>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">Phone:</dt>
                        <dd class="col-sm-7">
                            @if (!string.IsNullOrEmpty(Model.PhoneNumber))
                            {
                                <a href="tel:@Model.PhoneNumber" class="text-decoration-none">
                                    <i class="bi bi-telephone"></i> @Model.PhoneNumber
                                </a>
                            }
                            else
                            {
                                <span class="text-muted">Not provided</span>
                            }
                        </dd>

                        <dt class="col-sm-5">Fax:</dt>
                        <dd class="col-sm-7">
                            @if (!string.IsNullOrEmpty(Model.FaxNumber))
                            {
                                <i class="bi bi-printer"></i> @Model.FaxNumber
                            }
                            else
                            {
                                <span class="text-muted">Not provided</span>
                            }
                        </dd>

                        <dt class="col-sm-5">Email:</dt>
                        <dd class="col-sm-7">
                            @if (!string.IsNullOrEmpty(Model.Email))
                            {
                                <a href="mailto:@Model.Email" class="text-decoration-none">
                                    <i class="bi bi-envelope"></i> @Model.Email
                                </a>
                            }
                            else
                            {
                                <span class="text-muted">Not provided</span>
                            }
                        </dd>
                    </dl>
                </div>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(Model.Address))
        {
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-geo-alt me-2"></i>
                            Address
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-0">@Model.Address</p>
                    </div>
                </div>
            </div>
        }

        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="bi bi-globe me-2"></i>
                        Online Access
                    </h5>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-3">Website:</dt>
                        <dd class="col-sm-9">
                            @if (!string.IsNullOrEmpty(Model.Website))
                            {
                                <a href="@Model.Website" target="_blank" class="text-decoration-none">
                                    @Model.Website <i class="bi bi-box-arrow-up-right small"></i>
                                </a>
                            }
                            else
                            {
                                <span class="text-muted">Not provided</span>
                            }
                        </dd>

                        <dt class="col-sm-3">Patient Portal:</dt>
                        <dd class="col-sm-9">
                            @if (!string.IsNullOrEmpty(Model.PortalUrl))
                            {
                                <a href="@Model.PortalUrl" target="_blank" class="btn btn-sm btn-primary">
                                    <i class="bi bi-box-arrow-up-right"></i> Access Portal
                                </a>
                            }
                            else
                            {
                                <span class="text-muted">Not provided</span>
                            }
                        </dd>
                    </dl>
                </div>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(Model.Notes))
        {
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-journal-text me-2"></i>
                            Notes
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-0">@Model.Notes</p>
                    </div>
                </div>
            </div>
        }
    </div>

    <div class="card mt-4">
        <div class="card-footer text-muted small">
            <div class="row">
                <div class="col-md-6">
                    Created: @Model.CreatedAt.ToString("MMM dd, yyyy 'at' h:mm tt")
                </div>
                @if (Model.UpdatedAt.HasValue)
                {
                    <div class="col-md-6 text-md-end">
                        Last Updated: @Model.UpdatedAt.Value.ToString("MMM dd, yyyy 'at' h:mm tt")
                    </div>
                }
            </div>
        </div>
    </div>
</div>
