@model HLE.FamilyHealth.Models.Entities.FamilyMember
@{
    ViewData["Title"] = "Family Member Details";
    var age = DateTime.Now.Year - Model.DateOfBirth.Year;
    if (DateTime.Now.DayOfYear < Model.DateOfBirth.DayOfYear) age--;
}

<div class="container-fluid py-4">
    <div class="d-flex align-items-center mb-4">
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
            <i class="bi bi-arrow-left"></i>
        </a>
        <div class="flex-grow-1">
            <div class="d-flex align-items-center justify-content-between">
                <div>
                    <h2 class="mb-1">@Model.FirstName @Model.LastName</h2>
                    <p class="text-muted mb-0">
                        @(Model.Relationship ?? "Family Member") | Age @age
                        @if (Model.IsActive)
                        {
                            ViewData["BadgeText"] = "Active";
                            ViewData["BadgeVariant"] = "success";
                            ViewData["BadgePill"] = "True";
                            <partial name="_Badge" />
                        }
                        else
                        {
                            ViewData["BadgeText"] = "Inactive";
                            ViewData["BadgeVariant"] = "secondary";
                            ViewData["BadgePill"] = "True";
                            <partial name="_Badge" />
                        }
                    </p>
                </div>
                <div>
                    @{
                        ViewData["Text"] = "Edit";
                        ViewData["Variant"] = "primary";
                        ViewData["Href"] = Url.Action("Edit", new { id = Model.Id });
                        ViewData["Icon"] = "bi bi-pencil";
                    }
                    <partial name="_Button" />
                </div>
            </div>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">Personal Information</h5>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-5">First Name:</dt>
                        <dd class="col-sm-7">@Model.FirstName</dd>

                        <dt class="col-sm-5">Last Name:</dt>
                        <dd class="col-sm-7">@Model.LastName</dd>

                        <dt class="col-sm-5">Date of Birth:</dt>
                        <dd class="col-sm-7">@Model.DateOfBirth.ToString("MMM dd, yyyy")</dd>

                        <dt class="col-sm-5">Age:</dt>
                        <dd class="col-sm-7">@age years</dd>

                        <dt class="col-sm-5">Gender:</dt>
                        <dd class="col-sm-7">@(Model.Gender ?? "-")</dd>

                        <dt class="col-sm-5">Relationship:</dt>
                        <dd class="col-sm-7">@(Model.Relationship ?? "-")</dd>

                        <dt class="col-sm-5">Status:</dt>
                        <dd class="col-sm-7">
                            @if (Model.IsActive)
                            {
                                ViewData["BadgeText"] = "Active";
                                ViewData["BadgeVariant"] = "success";
                                <partial name="_Badge" />
                            }
                            else
                            {
                                ViewData["BadgeText"] = "Inactive";
                                ViewData["BadgeVariant"] = "secondary";
                                <partial name="_Badge" />
                            }
                        </dd>
                    </dl>
                </div>
                <div class="card-footer text-muted small">
                    <div>Created: @Model.CreatedAt.ToString("MMM dd, yyyy")</div>
                    @if (Model.UpdatedAt.HasValue)
                    {
                        <div>Updated: @Model.UpdatedAt.Value.ToString("MMM dd, yyyy")</div>
                    }
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Health Profile</h5>
                    @if (Model.HealthProfile != null)
                    {
                        <a href="@Url.Action("Edit", "HealthProfiles", new { id = Model.HealthProfile.Id })" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                    }
                    else
                    {
                        <a href="@Url.Action("Create", "HealthProfiles", new { familyMemberId = Model.Id })" class="btn btn-sm btn-primary">
                            <i class="bi bi-plus"></i> Add Profile
                        </a>
                    }
                </div>
                <div class="card-body">
                    @if (Model.HealthProfile != null)
                    {
                        <dl class="row mb-0">
                            <dt class="col-sm-4">Blood Type:</dt>
                            <dd class="col-sm-8">
                                @if (!string.IsNullOrEmpty(Model.HealthProfile.BloodType))
                                {
                                    ViewData["BadgeText"] = Model.HealthProfile.BloodType;
                                    ViewData["BadgeVariant"] = "danger";
                                    <partial name="_Badge" />
                                }
                                else
                                {
                                    <span class="text-muted">Not specified</span>
                                }
                            </dd>

                            <dt class="col-sm-4">Height:</dt>
                            <dd class="col-sm-8">@(Model.HealthProfile.HeightCm?.ToString("F1") ?? "-") cm</dd>

                            <dt class="col-sm-4">Weight:</dt>
                            <dd class="col-sm-8">@(Model.HealthProfile.WeightKg?.ToString("F1") ?? "-") kg</dd>

                            <dt class="col-sm-4">Primary Care:</dt>
                            <dd class="col-sm-8">@(Model.HealthProfile.PrimaryCareProvider ?? "-")</dd>

                            <dt class="col-sm-4">Hospital:</dt>
                            <dd class="col-sm-8">@(Model.HealthProfile.PreferredHospital ?? "-")</dd>

                            <dt class="col-sm-4">Organ Donor:</dt>
                            <dd class="col-sm-8">
                                @if (Model.HealthProfile.IsOrganDonor)
                                {
                                    ViewData["BadgeText"] = "Yes";
                                    ViewData["BadgeVariant"] = "success";
                                    <partial name="_Badge" />
                                }
                                else
                                {
                                    ViewData["BadgeText"] = "No";
                                    ViewData["BadgeVariant"] = "secondary";
                                    <partial name="_Badge" />
                                }
                            </dd>
                        </dl>

                        @if (!string.IsNullOrEmpty(Model.HealthProfile.Allergies))
                        {
                            <div class="mt-3">
                                <strong>Allergies:</strong>
                                <p class="mb-0 text-muted">@Model.HealthProfile.Allergies</p>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(Model.HealthProfile.ChronicConditions))
                        {
                            <div class="mt-3">
                                <strong>Chronic Conditions:</strong>
                                <p class="mb-0 text-muted">@Model.HealthProfile.ChronicConditions</p>
                            </div>
                        }

                        <div class="mt-3">
                            <a href="@Url.Action("Details", "HealthProfiles", new { id = Model.HealthProfile.Id })" class="btn btn-sm btn-outline-primary">
                                View Full Profile <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-muted mb-3">No health profile found</p>
                            <a href="@Url.Action("Create", "HealthProfiles", new { familyMemberId = Model.Id })" class="btn btn-primary">
                                <i class="bi bi-plus"></i> Create Health Profile
                            </a>
                        </div>
                    }
                </div>
            </div>

            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Emergency Contacts (@Model.EmergencyContacts.Count)</h5>
                    <a href="@Url.Action("Create", "EmergencyContacts", new { familyMemberId = Model.Id })" class="btn btn-sm btn-primary">
                        <i class="bi bi-plus"></i> Add Contact
                    </a>
                </div>
                <div class="card-body">
                    @if (Model.EmergencyContacts.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var contact in Model.EmergencyContacts.OrderBy(c => c.Priority))
                            {
                                <div class="list-group-item px-0">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <h6 class="mb-1">@contact.Name</h6>
                                            <p class="mb-1 text-muted small">@contact.Relationship</p>
                                            <p class="mb-0 small">
                                                <i class="bi bi-telephone"></i> @contact.PhoneNumber
                                            </p>
                                        </div>
                                        <div>
                                            @{
                                                ViewData["BadgeText"] = $"Priority {contact.Priority}";
                                                ViewData["BadgeVariant"] = contact.Priority == 1 ? "danger" : "secondary";
                                                ViewData["BadgePill"] = "True";
                                            }
                                            <partial name="_Badge" />
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="mt-3">
                            <a href="@Url.Action("Index", "EmergencyContacts", new { familyMemberId = Model.Id })" class="btn btn-sm btn-outline-primary">
                                View All Contacts <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-muted mb-3">No emergency contacts found</p>
                            <a href="@Url.Action("Create", "EmergencyContacts", new { familyMemberId = Model.Id })" class="btn btn-primary">
                                <i class="bi bi-plus"></i> Add Emergency Contact
                            </a>
                        </div>
                    }
                </div>
            </div>

            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Insurance Policies (@Model.InsurancePolicies.Count)</h5>
                    <a href="@Url.Action("Create", "Insurance", new { familyMemberId = Model.Id })" class="btn btn-sm btn-primary">
                        <i class="bi bi-plus"></i> Add Policy
                    </a>
                </div>
                <div class="card-body">
                    @if (Model.InsurancePolicies.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var insurance in Model.InsurancePolicies.OrderByDescending(i => i.IsActive))
                            {
                                <div class="list-group-item px-0">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <h6 class="mb-1">@insurance.ProviderName</h6>
                                            <p class="mb-1 text-muted small">@insurance.InsuranceType</p>
                                            <p class="mb-0 small">
                                                Policy: @insurance.PolicyNumber
                                            </p>
                                            @if (insurance.ExpirationDate.HasValue)
                                            {
                                                <p class="mb-0 small text-muted">
                                                    Expires: @insurance.ExpirationDate.Value.ToString("MMM dd, yyyy")
                                                </p>
                                            }
                                        </div>
                                        <div>
                                            @if (insurance.IsActive)
                                            {
                                                ViewData["BadgeText"] = "Active";
                                                ViewData["BadgeVariant"] = "success";
                                                ViewData["BadgePill"] = "True";
                                                <partial name="_Badge" />
                                            }
                                            else
                                            {
                                                ViewData["BadgeText"] = "Inactive";
                                                ViewData["BadgeVariant"] = "secondary";
                                                ViewData["BadgePill"] = "True";
                                                <partial name="_Badge" />
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <div class="mt-3">
                            <a href="@Url.Action("Index", "Insurance", new { familyMemberId = Model.Id })" class="btn btn-sm btn-outline-primary">
                                View All Policies <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-muted mb-3">No insurance policies found</p>
                            <a href="@Url.Action("Create", "Insurance", new { familyMemberId = Model.Id })" class="btn btn-primary">
                                <i class="bi bi-plus"></i> Add Insurance Policy
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
