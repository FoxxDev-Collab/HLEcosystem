@model HLE.FamilyHealth.Models.Entities.Appointment
@{
    ViewData["Title"] = "Appointment Details";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Appointment Details</h2>
            <p class="text-muted mb-0">View appointment information</p>
        </div>
        <div class="d-flex gap-2">
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                <i class="bi bi-pencil"></i> Edit
            </a>
            @if (Model.VisitSummary == null && Model.Status == "Scheduled")
            {
                <form asp-action="MarkComplete" asp-route-id="@Model.Id" method="post" class="d-inline">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-success" onclick="return confirm('Mark this appointment as completed?')">
                        <i class="bi bi-check-circle"></i> Mark Complete
                    </button>
                </form>
            }
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    <div class="row">
        <div class="col-lg-8">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Appointment Information</h5>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Date & Time</label>
                            <p class="fw-bold">@Model.AppointmentDateTime.ToString("MMMM dd, yyyy 'at' h:mm tt")</p>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-muted">Duration</label>
                            <p class="fw-bold">@(Model.DurationMinutes.HasValue ? $"{Model.DurationMinutes} minutes" : "Not specified")</p>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Family Member</label>
                            <p class="fw-bold">
                                <a asp-controller="FamilyMembers" asp-action="Details" asp-route-id="@Model.FamilyMemberId">
                                    @Model.FamilyMember.FirstName @Model.FamilyMember.LastName
                                </a>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-muted">Provider</label>
                            <p class="fw-bold">
                                @if (Model.Provider != null)
                                {
                                    <a asp-controller="Providers" asp-action="Details" asp-route-id="@Model.ProviderId">
                                        @Model.Provider.Name
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">Not specified</span>
                                }
                            </p>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Appointment Type</label>
                            <p class="fw-bold">@Model.AppointmentType</p>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-muted">Status</label>
                            <p>
                                @{
                                    var statusClass = Model.Status switch
                                    {
                                        "Completed" => "success",
                                        "Cancelled" => "danger",
                                        "No-Show" => "warning",
                                        "Rescheduled" => "info",
                                        "Confirmed" => "primary",
                                        _ => "secondary"
                                    };
                                }
                                <span class="badge bg-@statusClass">@Model.Status</span>
                            </p>
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.Location))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Location</label>
                            <p class="fw-bold">@Model.Location</p>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.ReasonForVisit))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Reason for Visit</label>
                            <p>@Model.ReasonForVisit</p>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.PreAppointmentNotes))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Pre-Appointment Notes</label>
                            <p>@Model.PreAppointmentNotes</p>
                        </div>
                    }

                    <div class="row">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Reminder Sent</label>
                            <p>
                                @if (Model.ReminderSent)
                                {
                                    <span class="badge bg-success">
                                        <i class="bi bi-check-circle"></i> Yes
                                        @if (Model.ReminderSentAt.HasValue)
                                        {
                                            <span>(@Model.ReminderSentAt.Value.ToString("MMM dd, yyyy"))</span>
                                        }
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">No</span>
                                }
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Visit Summary Section -->
            @if (Model.VisitSummary != null)
            {
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0">
                            <i class="bi bi-file-medical"></i> Visit Summary
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-2">
                            This appointment has a recorded visit summary.
                        </p>
                        <a asp-controller="VisitSummaries" asp-action="Details" asp-route-id="@Model.VisitSummary.Id"
                           class="btn btn-success">
                            <i class="bi bi-eye"></i> View Visit Summary
                        </a>
                    </div>
                </div>
            }
            else if (Model.Status == "Completed" || Model.AppointmentDateTime < DateTime.Now)
            {
                <div class="card">
                    <div class="card-header bg-warning">
                        <h5 class="mb-0">
                            <i class="bi bi-exclamation-triangle"></i> No Visit Summary
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="mb-2">
                            This appointment doesn't have a visit summary yet. Add one to keep a record of what happened during the visit.
                        </p>
                        <a asp-controller="VisitSummaries" asp-action="Create" asp-route-appointmentId="@Model.Id"
                           class="btn btn-success">
                            <i class="bi bi-plus-circle"></i> Add Visit Summary
                        </a>
                    </div>
                </div>
            }
        </div>

        <div class="col-lg-4">
            <div class="card mb-4">
                <div class="card-header">
                    <h6 class="mb-0">Quick Actions</h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                            <i class="bi bi-pencil"></i> Edit Appointment
                        </a>
                        @if (Model.VisitSummary == null)
                        {
                            <a asp-controller="VisitSummaries" asp-action="Create" asp-route-appointmentId="@Model.Id"
                               class="btn btn-outline-success">
                                <i class="bi bi-file-medical"></i> Add Visit Summary
                            </a>
                        }
                        <a asp-action="Calendar" class="btn btn-outline-secondary">
                            <i class="bi bi-calendar3"></i> Back to Calendar
                        </a>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h6 class="mb-0">Danger Zone</h6>
                </div>
                <div class="card-body">
                    <p class="text-muted small mb-2">Delete this appointment permanently</p>
                    <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-danger w-100"
                                onclick="return confirm('Are you sure you want to delete this appointment? This action cannot be undone.')">
                            <i class="bi bi-trash"></i> Delete Appointment
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
