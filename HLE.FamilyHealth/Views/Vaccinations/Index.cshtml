@model HLE.FamilyHealth.Models.ViewModels.VaccinationIndexViewModel
@{
    ViewData["Title"] = "Vaccinations";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Vaccinations</h2>
            <p class="text-muted mb-0">Track vaccination records for family members</p>
        </div>
        @{
            ViewData["Text"] = "Add Vaccination";
            ViewData["Variant"] = "primary";
            ViewData["Href"] = Url.Action("Create");
            ViewData["Icon"] = "bi bi-plus";
        }
        <partial name="_Button" />
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    @if (Model.UpcomingDoses.Any())
    {
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0">Upcoming Doses (Next 60 Days)</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive table-mobile-cards-wrapper">
                    <table class="table-themed table-hover table-mobile-cards">
                        <thead>
                            <tr>
                                <th>Family Member</th>
                                <th>Vaccine</th>
                                <th>Dose</th>
                                <th>Due Date</th>
                                <th>Days Until</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var dose in Model.UpcomingDoses)
                            {
                                var badgeVariant = dose.DaysUntilNextDose <= 7 ? "warning" : "info";
                                <tr>
                                    <td data-label="Member" data-primary="true"><strong>@dose.FamilyMemberName</strong></td>
                                    <td data-label="Vaccine">@dose.VaccineName</td>
                                    <td data-label="Dose">@(dose.DoseNumber ?? "-")</td>
                                    <td data-label="Due Date">@dose.NextDoseDate?.ToString("MMM dd, yyyy")</td>
                                    <td data-label="Days Until">
                                        @{
                                            ViewData["BadgeText"] = $"{dose.DaysUntilNextDose} days";
                                            ViewData["BadgeVariant"] = badgeVariant;
                                        }
                                        <partial name="_Badge" />
                                    </td>
                                    <td class="text-end">
                                        <a href="@Url.Action("Details", new { id = dose.Id })" class="btn btn-outline-primary btn-sm">View</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    <div class="card">
        <div class="card-body">
            @if (!Model.Vaccinations.Any())
            {
                <div class="text-center py-5">
                    <p class="text-muted mb-3">No vaccinations recorded. Add vaccination records to track immunization history.</p>
                    <a href="@Url.Action("Create")" class="btn btn-primary">
                        <i class="bi bi-plus"></i> Add Vaccination
                    </a>
                </div>
            }
            else
            {
                <div class="table-responsive table-mobile-cards-wrapper">
                    <table class="table-themed table-hover table-mobile-cards">
                        <thead>
                            <tr>
                                <th>Family Member</th>
                                <th>Vaccine Name</th>
                                <th>Dose</th>
                                <th>Date Administered</th>
                                <th>Next Dose</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var vaccination in Model.Vaccinations)
                            {
                                <tr>
                                    <td data-label="Member" data-primary="true"><strong>@vaccination.FamilyMemberName</strong></td>
                                    <td data-label="Vaccine">@vaccination.VaccineName</td>
                                    <td data-label="Dose">@(vaccination.DoseNumber ?? "-")</td>
                                    <td data-label="Date">@vaccination.DateAdministered.ToString("MMM dd, yyyy")</td>
                                    <td data-label="Next Dose">
                                        @if (vaccination.NextDoseDate.HasValue)
                                        {
                                            @vaccination.NextDoseDate.Value.ToString("MMM dd, yyyy")
                                        }
                                        else
                                        {
                                            <span class="text-muted">-</span>
                                        }
                                    </td>
                                    <td class="text-end">
                                        <div class="btn-group btn-group-sm">
                                            <a href="@Url.Action("Details", new { id = vaccination.Id })" class="btn btn-outline-primary btn-sm">View</a>
                                            <a href="@Url.Action("Edit", new { id = vaccination.Id })" class="btn btn-outline-secondary btn-sm">Edit</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
