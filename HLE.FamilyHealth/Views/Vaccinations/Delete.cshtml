@model HLE.FamilyHealth.Models.Entities.Vaccination
@{
    ViewData["Title"] = "Delete Vaccination";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex align-items-center mb-4">
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <div>
                    <h2 class="mb-1">Delete Vaccination</h2>
                    <p class="text-muted mb-0">Confirm deletion of vaccination record</p>
                </div>
            </div>

            <div class="card border-danger">
                <div class="card-body">
                    <div class="alert alert-warning" role="alert">
                        <i class="bi bi-exclamation-triangle"></i>
                        Are you sure you want to delete this vaccination record? This action cannot be undone.
                    </div>

                    <dl class="row">
                        <dt class="col-sm-4">Family Member</dt>
                        <dd class="col-sm-8">@Model.FamilyMember.FirstName @Model.FamilyMember.LastName</dd>

                        <dt class="col-sm-4">Vaccine Name</dt>
                        <dd class="col-sm-8">@Model.VaccineName</dd>

                        <dt class="col-sm-4">Dose Number</dt>
                        <dd class="col-sm-8">@(Model.DoseNumber ?? "-")</dd>

                        <dt class="col-sm-4">Date Administered</dt>
                        <dd class="col-sm-8">@Model.DateAdministered.ToString("MMMM dd, yyyy")</dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@Model.Id" />

                        <div class="mt-4 d-flex gap-2">
                            @{
                                ViewData["Text"] = "Delete";
                                ViewData["Variant"] = "danger";
                                ViewData["Type"] = "submit";
                                ViewData["Icon"] = "bi bi-trash";
                            }
                            <partial name="_Button" />

                            @{
                                ViewData["Text"] = "Cancel";
                                ViewData["Variant"] = "outline-secondary";
                                ViewData["Href"] = Url.Action("Index");
                            }
                            <partial name="_Button" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
