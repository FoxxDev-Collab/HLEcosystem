@model HLE.FamilyHealth.Models.ViewModels.HealthProfileViewModel
@{
    ViewData["Title"] = "Edit Health Profile";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="d-flex align-items-center mb-4">
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <div>
                    <h2 class="mb-1">Edit Health Profile</h2>
                    <p class="text-muted mb-0">Update medical information@(Model.FamilyMemberName != null ? $" for {Model.FamilyMemberName}" : "")</p>
                </div>
            </div>

            @if (TempData["ErrorMessage"] != null)
            {
                ViewData["AlertType"] = "danger";
                ViewData["AlertMessage"] = TempData["ErrorMessage"]?.ToString();
                ViewData["AlertDismissible"] = true;
                <partial name="_Alert" />
            }

            <div class="card">
                <div class="card-body">
                    <form asp-action="Edit" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="FamilyMemberId" />
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="row g-3">
                            @if (!string.IsNullOrEmpty(Model.FamilyMemberName))
                            {
                                <div class="col-12">
                                    <div class="alert alert-info">
                                        <i class="bi bi-info-circle me-2"></i>
                                        Editing profile for <strong>@Model.FamilyMemberName</strong>
                                    </div>
                                </div>
                            }

                            <div class="col-12">
                                <h5>Basic Information</h5>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="BloodType" class="form-label">Blood Type</label>
                                <select asp-for="BloodType" class="form-select">
                                    <option value="">Select blood type</option>
                                    <option value="A+">A+</option>
                                    <option value="A-">A-</option>
                                    <option value="B+">B+</option>
                                    <option value="B-">B-</option>
                                    <option value="AB+">AB+</option>
                                    <option value="AB-">AB-</option>
                                    <option value="O+">O+</option>
                                    <option value="O-">O-</option>
                                </select>
                                <span asp-validation-for="BloodType" class="text-danger"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="HeightCm" class="form-label">Height (cm)</label>
                                <input asp-for="HeightCm" type="number" step="0.1" class="form-control" placeholder="e.g., 175.5" />
                                <span asp-validation-for="HeightCm" class="text-danger"></span>
                            </div>

                            <div class="col-md-4">
                                <label asp-for="WeightKg" class="form-label">Weight (kg)</label>
                                <input asp-for="WeightKg" type="number" step="0.1" class="form-control" placeholder="e.g., 70.5" />
                                <span asp-validation-for="WeightKg" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                                <h5>Medical Information</h5>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="Allergies" class="form-label">Allergies</label>
                                <textarea asp-for="Allergies" class="form-control" rows="4" placeholder="List any allergies (medications, foods, environmental, etc.)"></textarea>
                                <span asp-validation-for="Allergies" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="ChronicConditions" class="form-label">Chronic Conditions</label>
                                <textarea asp-for="ChronicConditions" class="form-control" rows="4" placeholder="List any chronic conditions or ongoing health issues"></textarea>
                                <span asp-validation-for="ChronicConditions" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <label asp-for="MajorSurgeries" class="form-label">Major Surgeries</label>
                                <textarea asp-for="MajorSurgeries" class="form-control" rows="3" placeholder="List any major surgeries with approximate dates"></textarea>
                                <span asp-validation-for="MajorSurgeries" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                                <h5>Care Providers</h5>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="PrimaryCareProvider" class="form-label">Primary Care Provider</label>
                                <input asp-for="PrimaryCareProvider" class="form-control" placeholder="Name of primary doctor" />
                                <span asp-validation-for="PrimaryCareProvider" class="text-danger"></span>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="PreferredHospital" class="form-label">Preferred Hospital</label>
                                <input asp-for="PreferredHospital" class="form-control" placeholder="Preferred hospital or medical center" />
                                <span asp-validation-for="PreferredHospital" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <label asp-for="MedicalNotes" class="form-label">Medical Notes</label>
                                <textarea asp-for="MedicalNotes" class="form-control" rows="4" placeholder="Any additional medical information or notes"></textarea>
                                <span asp-validation-for="MedicalNotes" class="text-danger"></span>
                            </div>

                            <div class="col-12">
                                <hr />
                            </div>

                            <div class="col-12">
                                <div class="form-check form-switch">
                                    <input asp-for="IsOrganDonor" class="form-check-input" type="checkbox" />
                                    <label asp-for="IsOrganDonor" class="form-check-label">Registered Organ Donor</label>
                                </div>
                                <span asp-validation-for="IsOrganDonor" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mt-4 d-flex gap-2">
                            @{
                                ViewData["Text"] = "Save Changes";
                                ViewData["Variant"] = "primary";
                                ViewData["Type"] = "submit";
                                ViewData["Icon"] = "bi bi-check-lg";
                            }
                            <partial name="_Button" />

                            @{
                                ViewData["Text"] = "Cancel";
                                ViewData["Variant"] = "outline-secondary";
                                ViewData["Href"] = Url.Action("Index");
                            }
                            <partial name="_Button" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
