@model HLE.FamilyHealth.Models.ViewModels.ExerciseCreateDto
@{
    ViewData["Title"] = "Add Exercise";
}

<div class="container-fluid py-4">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-1">
                <li class="breadcrumb-item"><a href="@Url.Action("Index")">Workouts</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Details", new { id = Model.WorkoutId })">@ViewBag.WorkoutTitle</a></li>
                <li class="breadcrumb-item active" aria-current="page">Add Exercise</li>
            </ol>
        </nav>
        <h2 class="mb-1">Add Exercise</h2>
        <p class="text-muted mb-0">Add an exercise to @ViewBag.WorkoutTitle</p>
    </div>

    <div class="card">
        <div class="card-body">
            <form asp-action="AddExercise" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="WorkoutId" />

                <div class="row mb-3">
                    <div class="col-md-8">
                        <label asp-for="ExerciseName" class="form-label"></label>
                        <input asp-for="ExerciseName" class="form-control" placeholder="e.g., Bench Press, Squat, Running" autofocus />
                        <span asp-validation-for="ExerciseName" class="text-danger"></span>
                    </div>
                    <div class="col-md-4">
                        <label asp-for="OrderIndex" class="form-label"></label>
                        <input asp-for="OrderIndex" type="number" class="form-control" />
                        <span asp-validation-for="OrderIndex" class="text-danger"></span>
                    </div>
                </div>

                <div class="row mb-3">
                    <div class="col-md-4">
                        <label asp-for="SupersetGroupId" class="form-label"></label>
                        <input asp-for="SupersetGroupId" type="number" class="form-control" placeholder="Optional - same number groups exercises" />
                        <span asp-validation-for="SupersetGroupId" class="text-danger"></span>
                        <div class="form-text">Exercises with the same superset group are performed together</div>
                    </div>
                    <div class="col-md-8">
                        <label asp-for="Notes" class="form-label"></label>
                        <input asp-for="Notes" class="form-control" placeholder="e.g., Single leg, Wide grip" />
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">Add Exercise</button>
                    <a href="@Url.Action("Details", new { id = Model.WorkoutId })" class="btn btn-outline-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
