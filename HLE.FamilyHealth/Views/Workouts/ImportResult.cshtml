@model HLE.FamilyHealth.Models.ViewModels.WorkoutImportResultViewModel
@{
    ViewData["Title"] = "Import Complete";
}

<div class="container-fluid py-4">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-1">
                <li class="breadcrumb-item"><a href="@Url.Action("Index")">Workouts</a></li>
                <li class="breadcrumb-item active" aria-current="page">Import Result</li>
            </ol>
        </nav>
        <h2 class="mb-1">Import Complete</h2>
        <p class="text-muted mb-0">Workout import has finished</p>
    </div>

    @if (Model.Success)
    {
        <div class="alert alert-success">
            <i class="bi bi-check-circle"></i>
            <strong>Import successful!</strong> Your workout data has been imported.
        </div>
    }
    else
    {
        <div class="alert alert-warning">
            <i class="bi bi-exclamation-triangle"></i>
            <strong>Import completed with errors.</strong> Some data may not have been imported.
        </div>
    }

    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card border-success">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Workouts Imported</h6>
                    <h3 class="mb-0 text-success">@Model.WorkoutsImported</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Exercises Imported</h6>
                    <h3 class="mb-0">@Model.ExercisesImported</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Sets Imported</h6>
                    <h3 class="mb-0">@Model.SetsImported</h3>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card @(Model.WorkoutsSkipped > 0 ? "border-warning" : "")">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Workouts Skipped</h6>
                    <h3 class="mb-0 @(Model.WorkoutsSkipped > 0 ? "text-warning" : "")">@Model.WorkoutsSkipped</h3>
                </div>
            </div>
        </div>
    </div>

    @if (Model.Errors.Any())
    {
        <div class="card mb-4">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0">Errors (@Model.Errors.Count)</h5>
            </div>
            <div class="card-body">
                <ul class="mb-0">
                    @foreach (var error in Model.Errors)
                    {
                        <li>@error</li>
                    }
                </ul>
            </div>
        </div>
    }

    <div class="d-flex gap-2">
        <a href="@Url.Action("Index")" class="btn btn-primary">
            <i class="bi bi-list"></i> View Workouts
        </a>
        <a href="@Url.Action("Import")" class="btn btn-outline-secondary">
            <i class="bi bi-upload"></i> Import More
        </a>
    </div>
</div>
