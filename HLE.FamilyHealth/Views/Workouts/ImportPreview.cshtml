@model HLE.FamilyHealth.Models.ViewModels.WorkoutImportPreviewViewModel
@{
    ViewData["Title"] = "Import Preview";
}

<div class="container-fluid py-4">
    <div class="mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-1">
                <li class="breadcrumb-item"><a href="@Url.Action("Index")">Workouts</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Import")">Import</a></li>
                <li class="breadcrumb-item active" aria-current="page">Preview</li>
            </ol>
        </nav>
        <h2 class="mb-1">Import Preview</h2>
        <p class="text-muted mb-0">Review workouts before importing</p>
    </div>

    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Family Member</h6>
                    <h4 class="mb-0">@Model.FamilyMemberName</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card border-success">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">New Workouts</h6>
                    <h4 class="mb-0 text-success">@Model.NewWorkoutsCount</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Total Exercises</h6>
                    <h4 class="mb-0">@Model.TotalExercises</h4>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <h6 class="text-muted mb-1">Total Sets</h6>
                    <h4 class="mb-0">@Model.TotalSets</h4>
                </div>
            </div>
        </div>
    </div>

    @if (Model.ExistingWorkoutsCount > 0)
    {
        <div class="alert alert-warning">
            <i class="bi bi-exclamation-triangle"></i>
            <strong>@Model.ExistingWorkoutsCount workout(s)</strong> already exist and will be skipped during import.
        </div>
    }

    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0">Workouts to Import</h5>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table-themed table-hover mb-0">
                    <thead>
                        <tr>
                            <th>Status</th>
                            <th>Date</th>
                            <th>Title</th>
                            <th>Duration</th>
                            <th>Exercises</th>
                            <th>Sets</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var workout in Model.Workouts.OrderByDescending(w => w.StartTime))
                        {
                            var duration = workout.EndTime.HasValue ? workout.EndTime.Value - workout.StartTime : (TimeSpan?)null;
                            <tr class="@(workout.AlreadyExists ? "table-secondary" : "")">
                                <td>
                                    @if (workout.AlreadyExists)
                                    {
                                        <span class="badge bg-secondary">Exists</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">New</span>
                                    }
                                </td>
                                <td>@workout.StartTime.ToString("MMM dd, yyyy h:mm tt")</td>
                                <td>@workout.Title</td>
                                <td>
                                    @if (duration.HasValue)
                                    {
                                        @(duration.Value.TotalHours >= 1 ? $"{duration.Value:h\\:mm}" : $"{duration.Value:m\\:ss}")
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>@workout.ExerciseCount</td>
                                <td>@workout.TotalSets</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="d-flex gap-2">
        @if (Model.NewWorkoutsCount > 0)
        {
            <form asp-action="ImportConfirm" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="FamilyMemberId" value="@Model.FamilyMemberId" />
                <input type="hidden" name="CachedDataKey" value="@Model.CachedDataKey" />
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-lg"></i> Import @Model.NewWorkoutsCount Workout(s)
                </button>
            </form>
        }
        <a href="@Url.Action("Import")" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Back
        </a>
        <a href="@Url.Action("Index")" class="btn btn-outline-secondary">Cancel</a>
    </div>
</div>
