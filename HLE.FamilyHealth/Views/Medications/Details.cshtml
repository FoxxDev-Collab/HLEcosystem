@model HLE.FamilyHealth.Models.Entities.Medication
@{
    ViewData["Title"] = "Medication Details";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex align-items-center mb-4">
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <div>
                    <h2 class="mb-1">Medication Details</h2>
                    <p class="text-muted mb-0">View medication record</p>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    <dl class="row">
                        <dt class="col-sm-4">Family Member</dt>
                        <dd class="col-sm-8">@Model.FamilyMember.FirstName @Model.FamilyMember.LastName</dd>

                        <dt class="col-sm-4">Medication Name</dt>
                        <dd class="col-sm-8">@Model.MedicationName</dd>

                        <dt class="col-sm-4">Dosage</dt>
                        <dd class="col-sm-8">@(Model.Dosage ?? "-")</dd>

                        <dt class="col-sm-4">Frequency</dt>
                        <dd class="col-sm-8">@(Model.Frequency ?? "-")</dd>

                        <dt class="col-sm-4">Status</dt>
                        <dd class="col-sm-8">
                            @if (Model.IsActive)
                            {
                                ViewData["BadgeText"] = "Active";
                                ViewData["BadgeVariant"] = "success";
                                <partial name="_Badge" />
                            }
                            else
                            {
                                ViewData["BadgeText"] = "Inactive";
                                ViewData["BadgeVariant"] = "secondary";
                                <partial name="_Badge" />
                            }
                        </dd>

                        <dt class="col-sm-4">Start Date</dt>
                        <dd class="col-sm-8">@(Model.StartDate?.ToString("MMMM dd, yyyy") ?? "-")</dd>

                        <dt class="col-sm-4">End Date</dt>
                        <dd class="col-sm-8">@(Model.EndDate?.ToString("MMMM dd, yyyy") ?? "-")</dd>

                        <dt class="col-sm-4">Prescribed By</dt>
                        <dd class="col-sm-8">@(Model.PrescribedBy ?? "-")</dd>

                        <dt class="col-sm-4">Pharmacy</dt>
                        <dd class="col-sm-8">@(Model.Pharmacy ?? "-")</dd>

                        <dt class="col-sm-4">Last Refill Date</dt>
                        <dd class="col-sm-8">@(Model.LastRefillDate?.ToString("MMMM dd, yyyy") ?? "-")</dd>

                        <dt class="col-sm-4">Next Refill Date</dt>
                        <dd class="col-sm-8">@(Model.NextRefillDate?.ToString("MMMM dd, yyyy") ?? "-")</dd>

                        <dt class="col-sm-4">Refills Remaining</dt>
                        <dd class="col-sm-8">@(Model.RefillsRemaining?.ToString() ?? "-")</dd>

                        <dt class="col-sm-4">Purpose</dt>
                        <dd class="col-sm-8">@(Model.Purpose ?? "-")</dd>

                        <dt class="col-sm-4">Side Effects</dt>
                        <dd class="col-sm-8">@(Model.SideEffects ?? "-")</dd>

                        <dt class="col-sm-4">Notes</dt>
                        <dd class="col-sm-8">@(Model.Notes ?? "-")</dd>

                        <dt class="col-sm-4">Created</dt>
                        <dd class="col-sm-8">@Model.CreatedAt.ToString("MMMM dd, yyyy")</dd>

                        @if (Model.UpdatedAt.HasValue)
                        {
                            <dt class="col-sm-4">Last Updated</dt>
                            <dd class="col-sm-8">@Model.UpdatedAt.Value.ToString("MMMM dd, yyyy")</dd>
                        }
                    </dl>

                    <div class="mt-4 d-flex gap-2">
                        @{
                            ViewData["Text"] = "Edit";
                            ViewData["Variant"] = "primary";
                            ViewData["Href"] = Url.Action("Edit", new { id = Model.Id });
                            ViewData["Icon"] = "bi bi-pencil";
                        }
                        <partial name="_Button" />

                        <form asp-action="ToggleActive" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@Model.Id" />
                            @{
                                ViewData["Text"] = Model.IsActive ? "Mark Inactive" : "Mark Active";
                                ViewData["Variant"] = "outline-secondary";
                                ViewData["Type"] = "submit";
                            }
                            <partial name="_Button" />
                        </form>

                        <form asp-action="Delete" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="id" value="@Model.Id" />
                            @{
                                ViewData["Text"] = "Delete";
                                ViewData["Variant"] = "danger";
                                ViewData["Type"] = "submit";
                                ViewData["Icon"] = "bi bi-trash";
                            }
                            <partial name="_Button" />
                        </form>

                        @{
                            ViewData["Text"] = "Back to List";
                            ViewData["Variant"] = "outline-secondary";
                            ViewData["Href"] = Url.Action("Index");
                        }
                        <partial name="_Button" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
