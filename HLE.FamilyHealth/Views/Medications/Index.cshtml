@model HLE.FamilyHealth.Models.ViewModels.MedicationIndexViewModel
@{
    ViewData["Title"] = "Medications";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Medications</h2>
            <p class="text-muted mb-0">Track medications for family members</p>
        </div>
        @{
            ViewData["Text"] = "Add Medication";
            ViewData["Variant"] = "primary";
            ViewData["Href"] = Url.Action("Create");
            ViewData["Icon"] = "bi bi-plus";
        }
        <partial name="_Button" />
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    @if (Model.RefillAlerts.Any())
    {
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0">Refill Alerts (Next 14 Days)</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive table-mobile-cards-wrapper">
                    <table class="table-themed table-hover table-mobile-cards">
                        <thead>
                            <tr>
                                <th>Family Member</th>
                                <th>Medication</th>
                                <th>Dosage</th>
                                <th>Next Refill</th>
                                <th>Days Until</th>
                                <th>Refills Left</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var med in Model.RefillAlerts)
                            {
                                var badgeVariant = med.DaysUntilRefill <= 3 ? "danger" : "warning";
                                <tr>
                                    <td data-label="Member" data-primary="true"><strong>@med.FamilyMemberName</strong></td>
                                    <td data-label="Medication">@med.MedicationName</td>
                                    <td data-label="Dosage">@(med.Dosage ?? "-")</td>
                                    <td data-label="Next Refill">@med.NextRefillDate?.ToString("MMM dd, yyyy")</td>
                                    <td data-label="Days Until">
                                        @{
                                            ViewData["BadgeText"] = $"{med.DaysUntilRefill} days";
                                            ViewData["BadgeVariant"] = badgeVariant;
                                        }
                                        <partial name="_Badge" />
                                    </td>
                                    <td data-label="Refills Left">@(med.RefillsRemaining?.ToString() ?? "-")</td>
                                    <td class="text-end">
                                        <a href="@Url.Action("Details", new { id = med.Id })" class="btn btn-outline-primary btn-sm">View</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }

    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0">Active Medications</h5>
        </div>
        <div class="card-body">
            @if (!Model.ActiveMedications.Any())
            {
                <p class="text-muted mb-0">No active medications.</p>
            }
            else
            {
                <div class="table-responsive table-mobile-cards-wrapper">
                    <table class="table-themed table-hover table-mobile-cards">
                        <thead>
                            <tr>
                                <th>Family Member</th>
                                <th>Medication</th>
                                <th>Dosage</th>
                                <th>Frequency</th>
                                <th>Next Refill</th>
                                <th>Refills Left</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var med in Model.ActiveMedications)
                            {
                                <tr>
                                    <td data-label="Member" data-primary="true"><strong>@med.FamilyMemberName</strong></td>
                                    <td data-label="Medication">@med.MedicationName</td>
                                    <td data-label="Dosage">@(med.Dosage ?? "-")</td>
                                    <td data-label="Frequency">@(med.Frequency ?? "-")</td>
                                    <td data-label="Next Refill">@(med.NextRefillDate?.ToString("MMM dd, yyyy") ?? "-")</td>
                                    <td data-label="Refills Left">@(med.RefillsRemaining?.ToString() ?? "-")</td>
                                    <td class="text-end">
                                        <div class="btn-group btn-group-sm">
                                            <a href="@Url.Action("Details", new { id = med.Id })" class="btn btn-outline-primary btn-sm">View</a>
                                            <a href="@Url.Action("Edit", new { id = med.Id })" class="btn btn-outline-secondary btn-sm">Edit</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>

    @if (Model.InactiveMedications.Any())
    {
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">Inactive Medications</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive table-mobile-cards-wrapper">
                    <table class="table-themed table-mobile-cards">
                        <thead>
                            <tr>
                                <th>Family Member</th>
                                <th>Medication</th>
                                <th>Dosage</th>
                                <th>Frequency</th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var med in Model.InactiveMedications)
                            {
                                <tr>
                                    <td data-label="Member" data-primary="true"><strong>@med.FamilyMemberName</strong></td>
                                    <td data-label="Medication">@med.MedicationName</td>
                                    <td data-label="Dosage">@(med.Dosage ?? "-")</td>
                                    <td data-label="Frequency">@(med.Frequency ?? "-")</td>
                                    <td class="text-end">
                                        <a href="@Url.Action("Details", new { id = med.Id })" class="btn btn-outline-primary btn-sm">View</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
</div>
