@model HLE.FamilyHealth.Models.Entities.VisitSummary
@{
    ViewData["Title"] = "Visit Summary Details";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="mb-1">Visit Summary</h2>
            <p class="text-muted mb-0">@Model.VisitDate.ToString("MMMM dd, yyyy")</p>
        </div>
        <div class="d-flex gap-2">
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">
                <i class="bi bi-pencil"></i> Edit
            </a>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        ViewData["AlertType"] = "success";
        ViewData["AlertMessage"] = TempData["SuccessMessage"]?.ToString();
        ViewData["AlertDismissible"] = true;
        <partial name="_Alert" />
    }

    <div class="row">
        <div class="col-lg-8">
            <!-- Basic Information -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Basic Information</h5>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Visit Date</label>
                            <p class="fw-bold">@Model.VisitDate.ToString("MMMM dd, yyyy 'at' h:mm tt")</p>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-muted">Visit Type</label>
                            <p>
                                @if (!string.IsNullOrEmpty(Model.VisitType))
                                {
                                    <span class="badge bg-info">@Model.VisitType</span>
                                }
                                else
                                {
                                    <span class="text-muted">Not specified</span>
                                }
                            </p>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label class="form-label text-muted">Family Member</label>
                            <p class="fw-bold">
                                <a asp-controller="FamilyMembers" asp-action="Details" asp-route-id="@Model.FamilyMemberId">
                                    @Model.FamilyMember.FirstName @Model.FamilyMember.LastName
                                </a>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label text-muted">Provider</label>
                            <p class="fw-bold">
                                @if (Model.Provider != null)
                                {
                                    <a asp-controller="Providers" asp-action="Details" asp-route-id="@Model.ProviderId">
                                        @Model.Provider.Name
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">Not specified</span>
                                }
                            </p>
                        </div>
                    </div>

                    @if (Model.Appointment != null)
                    {
                        <div class="alert alert-info">
                            <i class="bi bi-link-45deg"></i>
                            <strong>Linked to Appointment:</strong>
                            <a asp-controller="Appointments" asp-action="Details" asp-route-id="@Model.AppointmentId">
                                View appointment details
                            </a>
                        </div>
                    }
                </div>
            </div>

            <!-- Visit Details -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Visit Details</h5>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.ChiefComplaint))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Chief Complaint</label>
                            <p>@Model.ChiefComplaint</p>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.Diagnosis))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Diagnosis</label>
                            <p style="white-space: pre-wrap;">@Model.Diagnosis</p>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.TreatmentProvided))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Treatment Provided</label>
                            <p style="white-space: pre-wrap;">@Model.TreatmentProvided</p>
                        </div>
                    }

                    @if (string.IsNullOrEmpty(Model.ChiefComplaint) && string.IsNullOrEmpty(Model.Diagnosis) && string.IsNullOrEmpty(Model.TreatmentProvided))
                    {
                        <p class="text-muted">No visit details recorded.</p>
                    }
                </div>
            </div>

            <!-- Prescriptions & Tests -->
            @if (!string.IsNullOrEmpty(Model.PrescriptionsWritten) || !string.IsNullOrEmpty(Model.LabTestsOrdered))
            {
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Prescriptions & Tests</h5>
                    </div>
                    <div class="card-body">
                        @if (!string.IsNullOrEmpty(Model.PrescriptionsWritten))
                        {
                            <div class="mb-3">
                                <label class="form-label text-muted">Prescriptions Written</label>
                                <p style="white-space: pre-wrap;">@Model.PrescriptionsWritten</p>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(Model.LabTestsOrdered))
                        {
                            <div class="mb-0">
                                <label class="form-label text-muted">Lab Tests Ordered</label>
                                <p style="white-space: pre-wrap;">@Model.LabTestsOrdered</p>
                            </div>
                        }
                    </div>
                </div>
            }

            <!-- Follow-up -->
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Follow-up</h5>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.FollowUpInstructions))
                    {
                        <div class="mb-3">
                            <label class="form-label text-muted">Follow-Up Instructions</label>
                            <p style="white-space: pre-wrap;">@Model.FollowUpInstructions</p>
                        </div>
                    }

                    @if (Model.NextVisitRecommended.HasValue)
                    {
                        <div class="mb-0">
                            <label class="form-label text-muted">Next Visit Recommended</label>
                            <p>
                                <strong>@Model.NextVisitRecommended.Value.ToString("MMMM dd, yyyy")</strong>
                                @{
                                    var daysUntil = (Model.NextVisitRecommended.Value.Date - DateTime.Now.Date).Days;
                                    if (daysUntil < 0)
                                    {
                                        <span class="badge bg-danger">Overdue</span>
                                    }
                                    else if (daysUntil <= 7)
                                    {
                                        <span class="badge bg-warning">@daysUntil days</span>
                                    }
                                    else if (daysUntil <= 30)
                                    {
                                        <span class="badge bg-info">@daysUntil days</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">in @daysUntil days</span>
                                    }
                                }
                            </p>
                        </div>
                    }

                    @if (string.IsNullOrEmpty(Model.FollowUpInstructions) && !Model.NextVisitRecommended.HasValue)
                    {
                        <p class="text-muted">No follow-up information recorded.</p>
                    }
                </div>
            </div>

            <!-- Additional Information -->
            @if (!string.IsNullOrEmpty(Model.AttachedDocuments) || !string.IsNullOrEmpty(Model.Notes))
            {
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Additional Information</h5>
                    </div>
                    <div class="card-body">
                        @if (!string.IsNullOrEmpty(Model.AttachedDocuments))
                        {
                            <div class="mb-3">
                                <label class="form-label text-muted">Attached Documents</label>
                                <p style="white-space: pre-wrap;">@Model.AttachedDocuments</p>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(Model.Notes))
                        {
                            <div class="mb-0">
                                <label class="form-label text-muted">Notes</label>
                                <p style="white-space: pre-wrap;">@Model.Notes</p>
                            </div>
                        }
                    </div>
                </div>
            }
        </div>

        <div class="col-lg-4">
            <div class="card mb-4">
                <div class="card-header">
                    <h6 class="mb-0">Quick Actions</h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                            <i class="bi bi-pencil"></i> Edit Visit Summary
                        </a>
                        @if (Model.Appointment != null)
                        {
                            <a asp-controller="Appointments" asp-action="Details" asp-route-id="@Model.AppointmentId"
                               class="btn btn-outline-secondary">
                                <i class="bi bi-calendar-event"></i> View Appointment
                            </a>
                        }
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-list"></i> All Visit Summaries
                        </a>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h6 class="mb-0">Danger Zone</h6>
                </div>
                <div class="card-body">
                    <p class="text-muted small mb-2">Delete this visit summary permanently</p>
                    <form asp-action="Delete" asp-route-id="@Model.Id" method="post">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-danger w-100"
                                onclick="return confirm('Are you sure you want to delete this visit summary? This action cannot be undone.')">
                            <i class="bi bi-trash"></i> Delete Visit Summary
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
