# Required. Same Postgres as the rest of the ecosystem; the app uses
# its own schema (`media`) inside the `foxxlab` database.
DATABASE_URL=postgresql://foxxlab_admin:CHANGE_ME@postgres:5432/foxxlab

# Optional. Defaults shown.
PORT=8090

# Where the library is mounted inside the container (set in compose.yaml).
# Override only if running outside compose.
# MEDIA_LIBRARY_PATH=/app/library

# Optional. Free v3 key at https://www.themoviedb.org/settings/api
# Without it, scans still run but no posters/synopses are filled in.
# TMDB_API_KEY=

# Production cookie hardening. Leave commented in dev.
# AUTH_DOMAIN=.zulupf.com
# SECURE_COOKIES=true
