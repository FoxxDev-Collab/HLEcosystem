@using HLE.FamilyFinance.Services.Interfaces
@using HLE.FamilyFinance.Models.Enums
@using HLE.FamilyFinance.Helpers
@model TaxYearDetailDto
@{
    ViewData["Title"] = $"Edit {Model.Year} Tax Year";
    var receivedCount = Model.Documents.Count(d => d.IsReceived);
    var totalCount = Model.Documents.Count;
    var filesCount = Model.Documents.Count(d => d.HasFile);
}

<div class="py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2><i class="bi-pencil me-2"></i>Edit @Model.Year Tax Year</h2>
            <p class="text-muted mb-0">Update tax year information, filing status, and refunds.</p>
        </div>
        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
            <i class="bi-arrow-left me-1"></i>Back to Details
        </a>
    </div>

    <form asp-action="Edit" method="post">
        <input type="hidden" name="id" value="@Model.Id" />
        <div class="row g-4">
            <!-- Left Column: Main Form -->
            <div class="col-lg-8">
                <!-- Basic Information -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi-info-circle me-2"></i>Basic Information</h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label">Tax Year</label>
                                <input class="form-control" type="text" value="@Model.Year" disabled />
                                <small class="text-muted">Year cannot be changed</small>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Filing Status</label>
                                <select name="FederalFilingStatus" asp-items="@(ViewData["FilingStatuses"] as SelectList)" class="form-select"></select>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">State</label>
                                <input name="State" class="form-control" value="@Model.State" maxlength="2" placeholder="e.g., CA, TX" />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Federal Filing -->
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi-flag me-2"></i>Federal Return</h5>
                        @if (Model.IsFederalFiled)
                        {
                            <span class="badge bg-success">Filed</span>
                        }
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-3">
                                <div class="form-check form-switch mt-3">
                                    <input name="IsFederalFiled" class="form-check-input" type="checkbox" id="federalFiled"
                                           @(Model.IsFederalFiled ? "checked" : "") value="true" />
                                    <label class="form-check-label" for="federalFiled">Filed</label>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Date Filed</label>
                                <input name="FederalFiledDate" type="date" class="form-control"
                                       value="@Model.FederalFiledDate?.ToString("yyyy-MM-dd")" />
                            </div>
                            <div class="col-md-5">
                                <label class="form-label">Refund / Owed</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input name="FederalRefund" type="text" class="form-control money-input"
                                           value="@Model.FederalRefund" placeholder="Positive = refund, Negative = owed" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- State Filing -->
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi-geo-alt me-2"></i>State Return</h5>
                        @if (Model.IsStateFiled)
                        {
                            <span class="badge bg-success">Filed</span>
                        }
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-3">
                                <div class="form-check form-switch mt-3">
                                    <input name="IsStateFiled" class="form-check-input" type="checkbox" id="stateFiled"
                                           @(Model.IsStateFiled ? "checked" : "") value="true" />
                                    <label class="form-check-label" for="stateFiled">Filed</label>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Date Filed</label>
                                <input name="StateFiledDate" type="date" class="form-control"
                                       value="@Model.StateFiledDate?.ToString("yyyy-MM-dd")" />
                            </div>
                            <div class="col-md-5">
                                <label class="form-label">Refund / Owed</label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input name="StateRefund" type="text" class="form-control money-input"
                                           value="@Model.StateRefund" placeholder="Positive = refund, Negative = owed" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Refund Received -->
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi-cash-stack me-2"></i>Refund Status</h5>
                        @if (Model.RefundReceived)
                        {
                            <span class="badge bg-success">Received</span>
                        }
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <div class="form-check form-switch mt-3">
                                    <input name="RefundReceived" class="form-check-input" type="checkbox" id="refundReceived"
                                           @(Model.RefundReceived ? "checked" : "") value="true" />
                                    <label class="form-check-label" for="refundReceived">Refund Received</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Date Received</label>
                                <input name="RefundReceivedDate" type="date" class="form-control"
                                       value="@Model.RefundReceivedDate?.ToString("yyyy-MM-dd")" />
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Notes -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi-sticky me-2"></i>Notes</h5>
                    </div>
                    <div class="card-body">
                        <textarea name="Notes" class="form-control" rows="3"
                                  placeholder="Any notes about this tax year...">@Model.Notes</textarea>
                    </div>
                </div>

                <!-- Actions -->
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="bi-check-lg me-1"></i>Save Changes
                    </button>
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary btn-lg">Cancel</a>
                </div>
            </div>

            <!-- Right Column: Summary & Documents -->
            <div class="col-lg-4">
                <!-- Quick Stats -->
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi-bar-chart me-2"></i>Summary</h5>
                    </div>
                    <div class="card-body">
                        <div class="row text-center g-3">
                            <div class="col-6">
                                <div class="border rounded p-3">
                                    <div class="fs-3 fw-bold text-primary">@receivedCount/@totalCount</div>
                                    <small class="text-muted">Documents Received</small>
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="border rounded p-3">
                                    <div class="fs-3 fw-bold text-success">@filesCount</div>
                                    <small class="text-muted">Files Uploaded</small>
                                </div>
                            </div>
                        </div>
                        @if (Model.TotalGrossIncome > 0)
                        {
                            <hr />
                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Total Gross Income</span>
                                <strong>$@Model.TotalGrossIncome.ToString("N2")</strong>
                            </div>
                            <div class="d-flex justify-content-between mb-2">
                                <span class="text-muted">Federal Withheld</span>
                                <strong>$@Model.TotalFederalWithheld.ToString("N2")</strong>
                            </div>
                            <div class="d-flex justify-content-between">
                                <span class="text-muted">State Withheld</span>
                                <strong>$@Model.TotalStateWithheld.ToString("N2")</strong>
                            </div>
                        }
                    </div>
                </div>

                <!-- Documents Preview -->
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi-file-earmark-text me-2"></i>Documents</h5>
                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-primary">
                            Manage
                        </a>
                    </div>
                    @if (Model.Documents.Any())
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var doc in Model.Documents.OrderBy(d => d.IsReceived).ThenBy(d => d.DocumentType).Take(6))
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center py-2">
                                    <div class="d-flex align-items-center">
                                        <i class="@IconHelper.GetTaxDocumentIcon(doc.DocumentType) me-2 text-muted"></i>
                                        <div>
                                            <div class="small fw-medium">@doc.DocumentType</div>
                                            <div class="text-muted" style="font-size: 0.75rem;">@doc.Issuer</div>
                                        </div>
                                    </div>
                                    <div class="d-flex align-items-center gap-1">
                                        @if (doc.HasFile)
                                        {
                                            <i class="bi-paperclip text-primary" title="File attached"></i>
                                        }
                                        @if (doc.IsReceived)
                                        {
                                            <span class="badge bg-success">Received</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning text-dark">Waiting</span>
                                        }
                                    </div>
                                </li>
                            }
                            @if (Model.Documents.Count > 6)
                            {
                                <li class="list-group-item text-center text-muted small">
                                    +@(Model.Documents.Count - 6) more documents
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="card-body text-center py-4">
                            <i class="bi-file-earmark-x fs-3 text-muted"></i>
                            <p class="text-muted small mb-0 mt-2">No documents added yet</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
<script>
    // Set initial filing status value
    document.querySelector('select[name="FederalFilingStatus"]').value = '@Model.FederalFilingStatus';
</script>
}
