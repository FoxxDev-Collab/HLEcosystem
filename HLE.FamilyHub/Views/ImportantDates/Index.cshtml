@model List<HLE.FamilyHub.Services.Interfaces.ImportantDateSummaryDto>
@{
    ViewData["Title"] = "Important Dates";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Important Dates", null, true)
    };
}
<partial name="_Breadcrumb" />

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>
        <i class="bi bi-calendar-heart"></i> Important Dates
    </h2>
    @{
        ViewData["Text"] = "Add Date";
        ViewData["Variant"] = "primary";
        ViewData["Icon"] = "bi-calendar-plus";
        ViewData["Href"] = Url.Action("Create");
    }
    <partial name="_Button" />
</div>

@if (Model.Any())
{
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Next Date</th>
                            <th>Label</th>
                            <th>Type</th>
                            <th>Family Member</th>
                            <th>Days Until</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var date in Model.OrderBy(d => d.DaysUntilNext))
                        {
                            <tr>
                                <td>@date.NextOccurrence.ToString("MMM d, yyyy")</td>
                                <td>
                                    <strong>@date.Label</strong>
                                </td>
                                <td>
                                    @{
                                        var typeIcon = date.Type switch
                                        {
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Birthday => "bi-cake2",
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Anniversary => "bi-heart",
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Holiday => "bi-calendar-star",
                                            _ => "bi-calendar-event"
                                        };
                                        var typeVariant = date.Type switch
                                        {
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Birthday => "primary",
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Anniversary => "danger",
                                            HLE.FamilyHub.Models.Enums.ImportantDateType.Holiday => "success",
                                            _ => "secondary"
                                        };
                                    }
                                    <i class="@typeIcon"></i>
                                    @{
                                        ViewData["Text"] = date.Type.ToString();
                                        ViewData["Variant"] = typeVariant;
                                    }
                                    <partial name="_Badge" />
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(date.FamilyMemberName))
                                    {
                                        @date.FamilyMemberName
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @{
                                        var daysUntil = date.DaysUntilNext;
                                        var badgeClass = daysUntil < 7 ? "bg-danger" : (daysUntil < 14 ? "bg-warning text-dark" : "bg-success");
                                    }
                                    <span class="badge @badgeClass">
                                        @if (daysUntil == 0)
                                        {
                                            <text>Today</text>
                                        }
                                        else if (daysUntil == 1)
                                        {
                                            <text>Tomorrow</text>
                                        }
                                        else
                                        {
                                            <text>@daysUntil days</text>
                                        }
                                    </span>
                                </td>
                                <td>
                                    <div class="d-flex gap-1">
                                        @{
                                            ViewData["Text"] = "Edit";
                                            ViewData["Variant"] = "outline-secondary";
                                            ViewData["Size"] = "sm";
                                            ViewData["Href"] = Url.Action("Edit", new { id = date.Id });
                                        }
                                        <partial name="_Button" />

                                        <form asp-action="Delete" asp-route-id="@date.Id" method="post" style="display:inline;"
                                              onsubmit="return confirm('Are you sure you want to delete this date?');">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-outline-danger">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="bi bi-calendar-x display-1 text-muted d-block mb-3"></i>
            <h4>No Important Dates</h4>
            <p class="text-muted">Add birthdays, anniversaries, and other important dates to keep track of.</p>
            @{
                ViewData["Text"] = "Add Your First Date";
                ViewData["Variant"] = "primary";
                ViewData["Icon"] = "bi-calendar-plus";
                ViewData["Href"] = Url.Action("Create");
            }
            <partial name="_Button" />
        </div>
    </div>
}
