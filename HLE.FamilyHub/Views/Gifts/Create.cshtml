@model HLE.FamilyHub.Models.ViewModels.GiftCreateViewModel
@{
    ViewData["Title"] = "Record Gift";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Gift History", Url.Action("Index"), false),
        ("Record", null, true)
    };
}
<partial name="_Breadcrumb" />

<h2>
    <i class="bi bi-gift"></i> Record Gift
</h2>

<div class="row">
    <div class="col-lg-6">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div class="card mb-3">
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="FamilyMemberId" class="form-label"></label>
                        <select asp-for="FamilyMemberId" class="form-select" required>
                            <option value="">Select family member...</option>
                            @if (ViewBag.FamilyMembers != null)
                            {
                                foreach (var member in ViewBag.FamilyMembers as List<HLE.FamilyHub.Services.Interfaces.FamilyMemberSummaryDto>)
                                {
                                    <option value="@member.Id">@member.FirstName @member.LastName</option>
                                }
                            }
                        </select>
                        <span asp-validation-for="FamilyMemberId" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label"></label>
                        <input asp-for="Description" class="form-control" placeholder="e.g., Bluetooth speaker, Cookbook" />
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="GiftDate" class="form-label"></label>
                        <input asp-for="GiftDate" type="date" class="form-control" />
                        <span asp-validation-for="GiftDate" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Occasion" class="form-label"></label>
                        <input asp-for="Occasion" class="form-control" placeholder="e.g., Birthday, Christmas, Anniversary" />
                        <span asp-validation-for="Occasion" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Status" class="form-label"></label>
                        <select asp-for="Status" class="form-select">
                            <option value="Idea">Idea</option>
                            <option value="Purchased" selected>Purchased</option>
                            <option value="Wrapped">Wrapped</option>
                            <option value="Given">Given</option>
                        </select>
                        <span asp-validation-for="Status" class="text-danger"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="EstimatedCost" class="form-label"></label>
                            <input asp-for="EstimatedCost" type="number" step="0.01" min="0" class="form-control" placeholder="0.00" />
                            <span asp-validation-for="EstimatedCost" class="text-danger"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="ActualCost" class="form-label"></label>
                            <input asp-for="ActualCost" type="number" step="0.01" min="0" class="form-control" placeholder="0.00" />
                            <span asp-validation-for="ActualCost" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Rating" class="form-label"></label>
                        <select asp-for="Rating" class="form-select">
                            <option value="">Not rated</option>
                            <option value="1">1 - Poor</option>
                            <option value="2">2 - Fair</option>
                            <option value="3">3 - Good</option>
                            <option value="4">4 - Very Good</option>
                            <option value="5">5 - Excellent</option>
                        </select>
                        <span asp-validation-for="Rating" class="text-danger"></span>
                        <div class="form-text">How well was this gift received?</div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Notes" class="form-label"></label>
                        <textarea asp-for="Notes" class="form-control" rows="4" placeholder="Any additional details about the gift..."></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2 mb-4">
                @{
                    ViewData["Text"] = "Cancel";
                    ViewData["Variant"] = "secondary";
                    ViewData["Href"] = Url.Action("Index");
                }
                <partial name="_Button" />

                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-circle"></i> Save Gift
                </button>
            </div>
        </form>
    </div>

    <div class="col-lg-6">
        <div class="alert alert-info">
            <h5 class="alert-heading">
                <i class="bi bi-info-circle"></i> Why Track Gifts?
            </h5>
            <ul class="mb-0">
                <li>Build a history to avoid repeating gifts</li>
                <li>Track spending across occasions and family members</li>
                <li>Remember what worked well (use ratings!)</li>
                <li>Reference notes for future gift ideas</li>
            </ul>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
