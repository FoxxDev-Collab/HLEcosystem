@model HLE.FamilyHub.Models.ViewModels.GiftIdeaEditViewModel
@{
    ViewData["Title"] = "Edit Gift Idea";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Gift Ideas", Url.Action("Index"), false),
        ("Edit", null, true)
    };
}
<partial name="_Breadcrumb" />

<h2>
    <i class="bi bi-pencil"></i> Edit Gift Idea
</h2>

<div class="row">
    <div class="col-lg-6">
        <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
            @Html.AntiForgeryToken()

            <div class="card mb-3">
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="Idea" class="form-label"></label>
                        <input asp-for="Idea" class="form-control" />
                        <span asp-validation-for="Idea" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="FamilyMemberId" class="form-label"></label>
                        <select asp-for="FamilyMemberId" class="form-select">
                            <option value="">General Idea (assign later)</option>
                            @if (ViewBag.FamilyMembers != null)
                            {
                                foreach (var member in ViewBag.FamilyMembers as List<HLE.FamilyHub.Services.Interfaces.FamilyMemberSummaryDto>)
                                {
                                    <option value="@member.Id">@member.FirstName @member.LastName</option>
                                }
                            }
                        </select>
                        <span asp-validation-for="FamilyMemberId" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Source" class="form-label"></label>
                        <input asp-for="Source" class="form-control" />
                        <span asp-validation-for="Source" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Priority" class="form-label"></label>
                        <select asp-for="Priority" class="form-select">
                            <option value="Low">Low</option>
                            <option value="Medium">Medium</option>
                            <option value="High">High</option>
                        </select>
                        <span asp-validation-for="Priority" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="EstimatedCost" class="form-label"></label>
                        <input asp-for="EstimatedCost" type="number" step="0.01" min="0" class="form-control" />
                        <span asp-validation-for="EstimatedCost" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Url" class="form-label"></label>
                        <input asp-for="Url" type="url" class="form-control" />
                        <span asp-validation-for="Url" class="text-danger"></span>
                        <div class="form-text">Link to product page or wishlist</div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Notes" class="form-label"></label>
                        <textarea asp-for="Notes" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2 mb-4">
                @{
                    ViewData["Text"] = "Cancel";
                    ViewData["Variant"] = "secondary";
                    ViewData["Href"] = Url.Action("Index");
                }
                <partial name="_Button" />

                <button type="submit" class="btn btn-success">
                    <i class="bi bi-check-circle"></i> Save Changes
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
