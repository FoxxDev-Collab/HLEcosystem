@model List<HLE.FamilyHub.Services.Interfaces.FamilyMemberDetailDto>
@{
    ViewData["Title"] = "Address Book";
}

@{
    ViewData["BreadcrumbItems"] = new List<(string Text, string? Url, bool IsActive)>
    {
        ("Home", Url.Action("Index", "Home"), false),
        ("Address Book", null, true)
    };
}
<partial name="_Breadcrumb" />

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>
        <i class="bi bi-envelope"></i> Holiday Card Address Book
    </h2>
    <button onclick="window.print()" class="btn btn-primary">
        <i class="bi bi-printer"></i> Print
    </button>
</div>

@if (Model.Any())
{
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Address</th>
                            <th>Phone</th>
                            <th>Email</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var member in Model.OrderBy(m => m.LastName).ThenBy(m => m.FirstName))
                        {
                            <tr>
                                <td>
                                    <strong>@member.FirstName @member.LastName</strong>
                                    @if (!string.IsNullOrEmpty(member.Nickname))
                                    {
                                        <br />
                                        <span class="text-muted small">(@member.Nickname)</span>
                                    }
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(member.AddressLine1))
                                    {
                                        <address class="mb-0">
                                            @member.AddressLine1
                                            @if (!string.IsNullOrEmpty(member.AddressLine2))
                                            {
                                                <br />@member.AddressLine2
                                            }
                                            <br />
                                            @if (!string.IsNullOrEmpty(member.City) || !string.IsNullOrEmpty(member.State) || !string.IsNullOrEmpty(member.ZipCode))
                                            {
                                                <text>@member.City, @member.State @member.ZipCode</text>
                                            }
                                        </address>
                                    }
                                    else
                                    {
                                        <span class="text-muted">No address</span>
                                    }
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(member.Phone))
                                    {
                                        <a href="tel:@member.Phone">@member.Phone</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(member.Email))
                                    {
                                        <a href="mailto:@member.Email">@member.Email</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <div class="card">
        <div class="card-body text-center py-5">
            <i class="bi bi-envelope-x display-1 text-muted d-block mb-3"></i>
            <h4>No Addresses Available</h4>
            <p class="text-muted">Add family members with addresses to build your holiday card list.</p>
            @{
                ViewData["Text"] = "Add Family Member";
                ViewData["Variant"] = "primary";
                ViewData["Icon"] = "bi-person-plus";
                ViewData["Href"] = Url.Action("Create");
            }
            <partial name="_Button" />
        </div>
    </div>
}

<style>
    @@media print {
        .navbar,
        .breadcrumb,
        .btn,
        footer {
            display: none !important;
        }

        .card {
            border: none !important;
            box-shadow: none !important;
        }

        body {
            background: white !important;
        }

        .table {
            font-size: 12pt;
        }
    }
</style>
