@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="display-4">Welcome to Family Hub</h1>
    <p class="lead">Manage your family relationships, important dates, and gift ideas all in one place.</p>
</div>

<div class="row g-4 mt-4">
    <div class="col-md-4">
        <div class="card h-100 text-center">
            <div class="card-body">
                <div class="mb-3">
                    <i class="bi bi-speedometer2 display-1 text-primary"></i>
                </div>
                <h5 class="card-title">Dashboard</h5>
                <p class="card-text">View your upcoming events, recent activity, and quick stats</p>
                @{
                    ViewData["Text"] = "Go to Dashboard";
                    ViewData["Variant"] = "primary";
                    ViewData["Href"] = Url.Action("Dashboard");
                }
                <partial name="_Button" />
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card h-100 text-center">
            <div class="card-body">
                <div class="mb-3">
                    <i class="bi bi-people display-1 text-success"></i>
                </div>
                <h5 class="card-title">Family Members</h5>
                <p class="card-text">Manage contact information, birthdays, and addresses</p>
                @{
                    ViewData["Text"] = "View Family";
                    ViewData["Variant"] = "success";
                    ViewData["Href"] = Url.Action("Index", "FamilyMembers");
                }
                <partial name="_Button" />
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card h-100 text-center">
            <div class="card-body">
                <div class="mb-3">
                    <i class="bi bi-lightbulb display-1 text-warning"></i>
                </div>
                <h5 class="card-title">Gift Ideas</h5>
                <p class="card-text">Capture and track gift ideas throughout the year</p>
                @{
                    ViewData["Text"] = "View Ideas";
                    ViewData["Variant"] = "warning";
                    ViewData["Href"] = Url.Action("Index", "GiftIdeas");
                }
                <partial name="_Button" />
            </div>
        </div>
    </div>
</div>
