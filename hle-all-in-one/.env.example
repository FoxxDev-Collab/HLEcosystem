# Copy to .env (gitignored). Bun auto-loads .env.
# IMPORTANT: no ?schema= suffix — Bun.sql forwards unknown URL query params as
# Postgres startup options and the server rejects them. Single schema = public.
DATABASE_URL=postgresql://hle:hle_dev_pw@localhost:5433/hle_aio

# Dev seed admin (scripts/seed.ts). Change for anything non-local.
# e2e/auth.setup.ts reads the same two vars with the same defaults — change
# them here and the Playwright login follows.
SEED_ADMIN_EMAIL=admin@hle.local
SEED_ADMIN_PASSWORD=ChangeMe123!
SEED_ADMIN_FIRST_NAME=Admin
SEED_ADMIN_LAST_NAME=User

# Media scanner root. Unset → scans return "MEDIA_LIBRARY_PATH not configured".
MEDIA_LIBRARY_PATH=
# Optional TMDB v3 key. Unset → scanner runs without metadata enrichment.
TMDB_API_KEY=
# Optional AI gateway (both required together). Unset → receipt OCR, smart
# categorize, smart link, advisor, and list generation fall back to manual.
CLAUDE_API_URL=
CLAUDE_API_SERVICE_SECRET=
